# 세션 인수인계: 2026-04-03 #0002

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: SDS AX Advanced 교육 Mini PJT — Claude Code급 Coding AI Agent 구현
- **저장소**: /home/youngs75/projects/youngs75_coding_ai_agent
- **참조 프로젝트**: DeepAgents CLI (https://github.com/langchain-ai/deepagents/tree/main/libs/cli/deepagents_cli)

## 이번 세션 작업 내역

### Phase 1: 멀티티어 모델 해석 + OpenRouter 지원 (완료)
- `core/model_tiers.py` 신규 — ModelTier(STRONG/DEFAULT/FAST), TierConfig, resolve_tier_config(), create_chat_model()
- `core/config.py` 수정 — model_tiers/purpose_tiers 필드 추가, get_tier_config() 메서드 추가. 레거시 _resolve_model_name() 유지 (ResearchConfig 하위 호환)
- `agents/coding_assistant/config.py` 수정 — 티어 기반 get_model() 오버라이드. CODING_GEN_MODEL/CODING_VERIFY_MODEL 레거시 오버라이드 우선 적용
- `.env.example` 수정 — OpenRouter, 멀티티어, PURPOSE_TIERS 환경변수 추가
- `tests/test_model_tiers.py` 신규 — 23개 테스트 전체 통과

### Phase 2: 메모리 시스템 (완료)
- `core/memory/schemas.py` — MemoryType(Working/Episodic/Semantic/Procedural), MemoryItem
- `core/memory/search.py` — TagBasedSearch(1단계), ContentBasedSearch(BM25, 2단계), TwoStageSearch(GAM 이중 구조)
- `core/memory/store.py` — MemoryStore(CRUD + 2단계 검색 + 세션 스코프 + 타입별 관리)
- `core/memory/state.py` — MemoryAwareState(Working + Semantic + Episodic)
- `tests/test_memory.py` 신규 — 26개 테스트 전체 통과

### Phase 3: Skills 시스템 (완료)
- `core/skills/schemas.py` — Skill, SkillLevel(L1_METADATA/L2_BODY/L3_REFERENCES), SkillMetadata, SkillReference
- `core/skills/loader.py` — SkillLoader (YAML/JSON 파일에서 3-Level 로딩)
- `core/skills/registry.py` — SkillRegistry (탐색, 활성화, 태그 검색, 활성화 통계)
- `tests/test_skills.py` 신규 — 24개 테스트 전체 통과

### Phase 4: 동적 SubAgent (완료)
- `core/subagents/schemas.py` — SubAgentSpec, SubAgentStatus, SubAgentUsageRecord, SelectionResult
- `core/subagents/registry.py` — SubAgentRegistry (Puppeteer 패턴: R=r(quality)−λ·C(cost), 사용 통계 추적)
- `tests/test_subagents.py` 신규 — 12개 테스트 전체 통과

### Phase 5: 대화형 CLI (완료)
- `cli/app.py` — 대화형 메인 루프 (prompt-toolkit + rich)
- `cli/renderer.py` — Rich 기반 출력 렌더러 (마크다운, 스트리밍)
- `cli/commands.py` — 슬래시 커맨드 (/help, /agent, /quit 등)
- `cli/session.py` — CLI 세션 관리 (히스토리, 메모리 연동)
- `cli/config.py` — CLI 설정
- `cli/__main__.py` — python -m youngs75_a2a.cli 엔트리포인트
- `tests/test_cli.py` 신규 — 21개 테스트 전체 통과

### 인프라 작업
- `pyproject.toml` — [build-system] 추가, setuptools package-dir 매핑 (youngs75_a2a → .), youngs75-agent CLI 엔트리포인트, tiktoken/rich/prompt-toolkit 의존성 추가
- `core/__init__.py` — Phase 1~2 새 심볼 export

## 현재 상태
- **Phase 1~5 전체 구현 완료**
- 전체 테스트: **106 passed**, 1 failed (기존 a2a 네이밍 충돌 — 변경과 무관)
- 기존 테스트 회귀 없음 (test_step1_no_llm.py 7/8 통과, a2a_assembly만 기존 실패)

## 다음 단계

### 우선 과제
1. **에이전트 그래프와 CLI 연동** — cli/app.py의 _run_agent_turn()에서 실제 CodingAssistantAgent 그래프 호출 연결
2. **메모리 시스템 에이전트 통합** — CodingState에 MemoryAwareState 연동, 시맨틱 메모리를 프롬프트에 주입
3. **Skills 에이전트 통합** — SkillRegistry를 BaseAgentConfig에 연결, 스킬 컨텍스트를 시스템 프롬프트에 자동 주입

### 보류 과제
4. **a2a 네이밍 충돌 해결** — 로컬 `a2a/` 디렉토리가 `a2a-sdk` 패키지를 가림 (server.py ↔ server/ 패키지 충돌)
5. **Docker 통합** — docker-compose에 CLI 서비스 추가
6. **Episodic/Procedural 메모리 활성화** — 현재 스키마만 정의됨, 오류 전파 위험 평가 후 활성화

## 중요 참고사항
- **패키지 구조**: 디렉토리명 `youngs75_coding_ai_agent`이지만 import는 `youngs75_a2a`로 사용. pyproject.toml의 setuptools package-dir 매핑으로 해결. 패키지 변경 후 `uv pip install -e .` 필수.
- **레거시 호환**: BaseAgentConfig.get_model()은 _resolve_model_name() 레거시 경로 유지. ResearchConfig 등 기존 에이전트는 변경 없이 동작. CodingConfig만 티어 기반으로 전환.
- **테스트 실행**: `.venv/bin/python -m pytest tests/ -v`
- **CLI 실행**: `youngs75-agent` 또는 `python -m youngs75_a2a.cli`
- 교육 패턴 연결: CoALA(메모리), GAM(검색), Puppeteer(SubAgent), RubricRewards(Generator/Verifier), Agent-as-a-Judge(오류전파방지), DeepAgents CLI(Skills, CLI)
