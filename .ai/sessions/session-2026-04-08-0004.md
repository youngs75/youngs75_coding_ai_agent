# 세션 인수인계: 2026-04-08 #0004

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: DeepAgents 스타일 도구 기반 파일 저장 전환 — 마크다운 파싱 의존 제거
- **저장소**: /home/sds/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

### 1. DeepAgents 코드베이스 심층 분석 (/home/sds/projects/deepagents)
- **Structured Output 전략**: Pydantic `WriteFileSchema`로 `write_file(file_path, content)` 도구 스키마 강제
- **파일 저장 방식**: LLM이 `write_file` 도구를 호출 → MCP 서버가 즉시 디스크 저장 → 마크다운 파싱 불필요
- **PatchToolCallsMiddleware**: 미완성 도구 호출에 취소 ToolMessage 자동 삽입
- **핵심 발견**: DeepAgents는 코드 블록 파싱을 하지 않음 — 도구 스키마가 구조를 보장

### 2. Langfuse 검증 실패 원인 분석
- qwen3.5-plus 검증 모델이 `passed: false` 반환하는 근본 원인 5가지 확인:
  1. `# filepath:` 주석 형식 미준수 (규칙 8)
  2. SECRET_KEY 하드코딩 감지
  3. 코드 truncation으로 "미완성" 판정
  4. `__init__.py` 미생성
  5. 의존성 연결 불완전
- **규칙 8이 무한 재시도 루프의 핵심 원인**

### 3. DeepAgents 스타일 도구 기반 파일 저장 전환 (★ 핵심)

#### `agents/coding_assistant/prompts.py`
- **GENERATE_FINAL_SYSTEM_PROMPT 신규**: write_file 도구 사용 필수 지시, 마크다운 블록 금지 명시
- **VERIFY_SYSTEM_PROMPT 개정**: 규칙 8(filepath 주석) 삭제, 규칙 7(인용 품질) 삭제, 개발 환경 기본값 관대화

#### `agents/coding_assistant/agent.py`
- **`_generate_final()` 전면 개편**: 
  - STRONG 모델에 write_file + read_file 도구 바인딩
  - 내부 ReAct 루프 (최대 10회): LLM → write_file 호출 → ToolMessage → 다시 LLM
  - 도구 결과 "OK: path (N자, N줄)" 파싱으로 written_files 수집
  - 미완성 도구 호출 처리 (PatchToolCalls 패턴)
- **`_build_execute_system_prompt()` purpose 파라미터 추가**: "execute" vs "generate" 분기
- **`_apply_code()` 스킵 가드**: written_files 존재 시 마크다운 파싱 스킵, 없으면 기존 폴백
- **재시도 시 written_files 초기화**: `_verify_result()` passed=False 시, `_inject_test_failure()` 시
- **`_inject_test_failure()` 메시지 수정**: "filepath 주석 포함" → "write_file 도구로 저장하세요"

#### `tests/test_generate_tool_write.py` (신규 20개 테스트)
- GENERATE_FINAL_SYSTEM_PROMPT 검증 (6개)
- VERIFY_SYSTEM_PROMPT filepath 규칙 제거 확인 (6개)
- _apply_code 스킵 가드 (2개)
- written_files 초기화 (2개)
- write_file 결과 파싱 (4개)

#### `tests/test_cli.py` 기존 테스트 업데이트
- `test_verify_prompt_contains_citation_quality_check` → `test_verify_prompt_contains_quality_check`

### 4. Docker 재빌드
- agent-coding-assistant, orchestrator, mcp-server 3컨테이너 재빌드 완료
- 5컨테이너 모두 healthy

## 현재 상태

### 아키텍처 변경 요약
```
변경 전:
  LLM → 마크다운 텍스트 → _extract_code_blocks() 파싱 → 파일 저장
  검증: filepath 주석 형식 체크 → 무한 재시도

변경 후 (DeepAgents 패턴):
  LLM → write_file 도구 호출 → MCP 서버 즉시 저장 → 파싱 불필요
  검증: 코드 로직만 체크 (형식 무관)
  폴백: 도구 미호출 시 기존 마크다운 파싱 유지
```

### 테스트 결과
- 신규 테스트: 20/20 통과
- 기존 테스트: 918 passed (실패 9개는 LiteLLM 연결/프론트엔드 런타임 — 변경 무관)

### Docker Harness (5컨테이너, 모두 healthy)
```
litellm-db       — PostgreSQL 16
litellm-proxy    (:4000) — LLM Gateway + Langfuse
mcp-server       (:3003) — 통합 MCP (17개 도구, write_file 포함)
agent-coding     (:18084) — CodingAssistant (도구 기반 파일 저장)
orchestrator     (:18080) — Orchestrator
```

### 칸반보드 E2E 재테스트
- Docker 재빌드 + workspace 초기화 완료
- **사용자가 직접 테스트 진행 중**

## 다음 단계 (제안)

### TODO: 칸반보드 E2E 결과 분석 (최우선)
E2E 테스트 실행 완료. Phase 1 백엔드는 완벽하나 Phase 2/3 프론트엔드 파일 미생성 원인 분석 필요.

**E2E 결과 요약:**
- Phase 1 백엔드: ✅ write_file 9회 호출 → 8개 파일 저장, 검증 1회 통과, 테스트 통과
- Phase 2 프론트엔드 설정: ✅ 통과했지만 **0파일 저장** — write_file 미호출
- Phase 3 UI 컴포넌트: ✅ 통과했지만 **0파일 저장** — write_file 미호출
- 총 소요: 193초 (이전 5분+ 대비 대폭 개선)

**분석 포인트:**
1. Langfuse에서 Phase 2/3 트레이스 확인 — write_file 도구가 바인딩되었는지, LLM이 마크다운으로 폴백했는지
2. Orchestrator → CodingAssistant 서브에이전트 위임 시 MCP 도구 전달 여부 확인
3. Phase 2/3에서 `read_file`만 호출하고 `write_file` 미호출 원인 파악
4. 마크다운 폴백 경로로 갔다면 `_apply_code()`에서 파싱은 되었는지

### 추가 개선 (필요 시)
5. **str_replace/edit_file 도구 추가**: 기존 파일 수정 시 전체 재작성 대신 부분 수정
6. **미들웨어를 Orchestrator/Planner에도 확장**: 현재 CodingAssistant만 적용
7. **DeepAgents FilesystemMiddleware 패턴 도입**: 대용량 도구 결과 파일 오프로드

## 변경된 파일 (4개 수정 + 1개 신규)

**수정 (3개)**:
1. `agents/coding_assistant/agent.py` — _generate_final 도구 루프 + _apply_code 스킵 + purpose 파라미터
2. `agents/coding_assistant/prompts.py` — GENERATE_FINAL_SYSTEM_PROMPT + VERIFY 규칙 개정
3. `tests/test_cli.py` — 기존 테스트 업데이트

**신규 (1개)**:
4. `tests/test_generate_tool_write.py` — 도구 기반 파일 저장 테스트 20개

## 중요 참고사항

### 마크다운 파싱 폴백 유지
- `_extract_code_blocks()` 함수와 기존 `_apply_code()` 파싱 로직은 삭제하지 않음
- LLM이 도구 호출 대신 마크다운을 출력할 경우 기존 경로로 폴백
- 점진적 전환을 위해 양쪽 경로 모두 활성

### DeepAgents 레퍼런스 위치
- `/home/sds/projects/deepagents/libs/deepagents/deepagents/middleware/filesystem.py` — WriteFileSchema, FilesystemMiddleware
- `/home/sds/projects/deepagents/libs/deepagents/deepagents/middleware/patch_tool_calls.py` — 미완성 도구 호출 패치
- 분석 문서: `/home/sds/projects/youngs75_coding_ai_agent/DeepAgents_Middleware_Analysis.md`
