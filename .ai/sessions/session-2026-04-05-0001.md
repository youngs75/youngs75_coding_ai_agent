# 세션 인수인계: 2026-04-05 #0001

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/youngs75/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

### 1. OpenRouter 도구 호출 최적화 — FAST→STRONG 2단계 파이프라인
- `core/model_tiers.py`: `tool_planning` purpose → FAST 티어 추가, capability 가중치 추가
- `agents/coding_assistant/config.py`: `tool_planning` → FAST 매핑 추가
- `agents/coding_assistant/agent.py`:
  - `_execute_code` 노드: FAST 모델로 도구 호출 판단 (ReAct 루프)
  - `_generate_final` 노드 신규: STRONG 모델로 최종 코드 생성
  - `_build_execute_system_prompt()`, `_build_context_message()` 공통 메서드 추출
  - `_get_tool_planning_model()` lazy init 추가
  - 라우팅: 도구 호출 있음 → EXECUTE_TOOLS, 도구 사용 이력 있음 → GENERATE_FINAL, 도구 미사용 → VERIFY (STRONG 생략)
- `cli/app.py`: 노드 레이블 업데이트 (`도구 호출 판단 (FAST)`, `코드 생성 (STRONG)`)

### 2. 스킬 자동 선택 — task_type 기반 자동 활성화
- `core/skills/registry.py`:
  - `TASK_TYPE_TAGS` 모듈 변수: task_type → 스킬 태그 매핑 (generate/fix/refactor/explain/analyze)
  - `auto_activate_for_task(task_type)`: 태그 매칭 → 자동 L2 활성화
  - `get_active_skill_bodies()`: L2 이상 활성화된 스킬 본문 반환
- `agents/coding_assistant/agent.py`:
  - `__init__`에 `skill_registry` 파라미터 추가
  - `_parse_request` 노드에서 parse 후 `auto_activate_for_task()` 호출, `skill_context`에 L2 본문 주입
- `cli/app.py`: `_create_agent()`에 `skill_registry` 전달, `_get_or_create_agent()`에서 세션 스킬 레지스트리 전달

### 3. CLI UX 개선 — 스킬/서브에이전트 피드백 표시
- `cli/renderer.py`: `skill_activated()`, `model_label()`, `subagent_delegate()` 메서드 추가
- `cli/app.py`:
  - `on_chain_end` 이벤트에서 `parse_request` 노드 완료 시 `⚙ 스킬 활성화: ...` 표시 (1회만)
  - Orchestrator 위임 시 `⇢ 위임: <에이전트명>` 표시
  - CLI 시작 시 `✓ 스킬 N개 로드: ...` 표시
  - `_init_skill_registry()` 반환 타입을 `tuple[SkillRegistry, list[str]]`로 변경

### 4. Orchestrator를 기본 에이전트로 변경
- `cli/config.py`: `default_agent` 기본값 `"coding_assistant"` → `"orchestrator"` 변경
- 모든 사용자 요청이 Orchestrator를 거쳐 적합한 Subagent로 자동 라우팅

### 5. LLM 타임아웃 추가 + Ctrl+C 예외 처리
- `core/model_tiers.py`:
  - `TierConfig`에 `request_timeout` 필드 추가 (기본 120초)
  - `build_default_tiers()`: STRONG=180s, DEFAULT=120s, FAST=60s (환경변수 오버라이드 가능)
  - `create_chat_model()`: `request_timeout` 적용
- `cli/app.py`: `asyncio.CancelledError` / `KeyboardInterrupt` 캐치 → `⚠ 작업이 중단되었습니다.` 표시 후 정상 복귀

### 6. 문서 전면 업데이트
- `README.md`: Orchestrator-First 아키텍처, 2단계 파이프라인, 스킬 자동 활성화, 현재 Qwen 모델 정보, CLI UX 예시, Skills/Subagents 섹션 추가
- `docs/ARCHITECTURE.md`: CodingAssistant 그래프 (generate_final/retrieve_memory 포함), Orchestrator 그래프 (coordinate 포함), 스킬 자동 활성화 다이어그램, CLI UX 피드백 표, 목적별 모델 매핑 업데이트
- `AGENTS.md`: 프로젝트 구조 업데이트 (core/skills/, core/memory/, data/skills/, cli/, docs/ 등)

### 7. 테스트 추가 및 기존 테스트 수정
- `tests/test_skills.py`: 자동 활성화 테스트 5건 추가 (fix→debug, explain→explain, unknown→empty, get_active_skill_bodies, auto_activate_updates_stats)
- `tests/test_model_tiers.py`: `tool_planning` purpose 테스트 추가
- `tests/test_cli.py`: `_create_agent` 시그니처 변경 반영 (skill_registry 추가), 노드 레이블 변경 반영 (`코드 생성` → `도구 호출 판단`), 기본 에이전트 변경 반영

## 현재 상태
- **Phase 1~14 + 추가 최적화 전체 구현 완료**
- 전체 단위 테스트: 750+ passed, 0 failures
- Non-LLM 테스트: 565 passed (2.44s)
- Ruff 린트: 변경 파일 전체 클린
- 모델: 전 티어 Qwen 시리즈 (STRONG/DEFAULT/FAST) + 타임아웃 설정
- 2단계 파이프라인: FAST(도구 호출) → STRONG(최종 생성), 도구 미사용 시 STRONG 생략
- 스킬: 7개 등록, task_type 기반 자동 활성화
- Orchestrator: 기본 에이전트, 3개 Subagent 자동 라우팅 + 복합 작업 coordinate
- 메모리: CoALA 4계층 전부 활성
- CLI: 스킬/서브에이전트 피드백 표시, Ctrl+C 안전 처리

## 다음 단계 후보
1. **Langfuse 대시보드 실제 확인** — 클라우드 트레이스 확인, 설정 정합성 검증
2. **Prime Jennie 연동 설계** — Self-evolving 코딩 하네스 API 인터페이스
3. **Orchestrator 실사용 테스트** — 복합 작업(coordinate) E2E 검증
4. **스킬 확장** — 더 많은 스킬 YAML 추가 (architecture_review, performance_review 등)
5. **A2A 엔드포인트 활성화** — 로컬 폴백이 아닌 실제 A2A HTTP 위임 테스트

## 중요 참고사항
- **모델 (Qwen 시리즈)**: STRONG: qwen/qwen3-coder-plus (1M, 180s), DEFAULT: qwen/qwen3-coder-next (262K, 120s), FAST: qwen/qwen3.5-flash-02-23 (1M, 60s)
- **기본 에이전트**: orchestrator (모든 요청 자동 라우팅)
- **패키지 구조**: import는 `youngs75_a2a`. 패키지 변경 후 `uv pip install -e .` 필수
- **a2a 디렉토리**: `a2a_local/`로 리네이밍됨 (a2a-sdk 충돌 방지)
- **테스트 실행**: `source .venv/bin/activate && python -m pytest tests/ --ignore=tests/eval/ -v`
- **E2E 테스트**: `python -m pytest tests/test_e2e_phase11.py -v` (MCP + API 키 필요)
- **CLI 실행**: `youngs75-agent` (별도 터미널에서)
- **MCP 서버**: `cd docker && docker compose -f docker-compose.mcp.yml up -d`
- **Skills**: `/skill list`로 조회, `/skill activate <name>`으로 수동 활성화, task_type 기반 자동 활성화
- **Orchestrator**: 기본 에이전트, `/agent coding_assistant`로 직접 전환 가능
- **금지 경로**: `.claude/`, `.git/`, `__pycache__/`, `node_modules/` 쓰기 자동 DENY
- **타임아웃**: STRONG=180s, DEFAULT=120s, FAST=60s (환경변수로 오버라이드: STRONG_TIMEOUT 등)
- **Ctrl+C**: `⚠ 작업이 중단되었습니다.` 표시 후 CLI 프롬프트로 정상 복귀
- **2단계 파이프라인**: 도구 사용 시 → FAST→STRONG, 도구 미사용 시 → FAST→VERIFY (STRONG 생략)
- **스킬 자동 활성화**: generate→3개, fix→1개, refactor→1개, explain→1개, analyze→2개
