# 세션 인수인계: 2026-04-05 #0006

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/youngs75/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

### 1. Planner Agent 도입 (Claude Code Plan Agent 패턴)

독립적인 Planner Agent를 신규 생성하여 오케스트레이터에 통합:

#### 새 모듈 (`agents/planner/`)
| 파일 | 역할 |
|------|------|
| `agent.py` | PlannerAgent — analyze_task → explore_context → create_plan 그래프 |
| `config.py` | PlannerConfig — REASONING 티어, Read-only 도구만 허용 |
| `prompts.py` | 분석/탐색/계획 프롬프트 (구조화된 JSON 출력) |
| `schemas.py` | TaskPlan, Phase, PlannerState 스키마 |

#### 오케스트레이터 통합
- 그래프 플로우: `classify → plan → delegate → respond` (코딩 태스크)
- 비코딩 태스크는 `classify → delegate` 직행
- Planner 계획이 coding_assistant에 컨텍스트로 전달

### 2. 4-Tier 모델 체계 도입 + DashScope 직접 호출

#### REASONING 티어 추가 (3-tier → 4-tier)
| 티어 | 모델 (DashScope) | 용도 | 비용 |
|------|-----------------|------|------|
| **REASONING** | qwen-max | 계획/아키텍처 설계 | $1.2/$6 per 1M |
| **STRONG** | qwen-coder-plus | 코드 생성/도구 호출 | $0.46/$1.38 |
| **DEFAULT** | qwen-plus | 검증/분석 | $0.4/$1.2 |
| **FAST** | qwen-turbo | 파싱/분류 | $0.04/$0.08 |

#### DashScope 프로바이더 추가
- `LLM_PROVIDER=dashscope` 환경변수로 전체 전환
- OpenRouter 대비 레이턴시 **100배 이상 개선** (185초 → 1.1초)
- 워크스페이스 전용 URL 지원 (`DASHSCOPE_BASE_URL` 오버라이드)

### 3. 자동 파일 저장 (apply_code 노드)

- `_extract_code_blocks()` — 마크다운에서 filepath 주석 파싱
  - 코드 블록 내부: `# filepath: app.py`
  - 코드 블록 외부: `# filepath: app.py` (마크다운 제목), `**filepath: app.py**` (굵은 텍스트)
  - dotfile 지원: `.env`, `.gitignore`
- `apply_code` 노드 — verify 통과 후 디스크에 저장
  - workspace 경로 보안 (탈출 방지)
  - 금지 경로 체크 (`.git/`, `.claude/` 등)
  - 디렉토리 자동 생성

### 4. CLI UX 개선

| 항목 | Before | After |
|------|--------|-------|
| 프롬프트 | `[orchestrator] >` | ANSI 컬러 `[orchestrator] ❯` |
| 코드 출력 | 전문 스트리밍 (수백 줄) | `📁 app.py (+72 lines)` 요약 |
| 내부 JSON | Planner raw JSON 노출 | 스피너만 표시 (inclusion list) |
| 컨텍스트 | 표시 없음 | `📊 ░░░ 2,129+3,552 tok (1.7%)` |
| 파일 저장 | 표시 없음 | `📁 저장된 파일: ✓ app.py (+72 lines)` |

### 5. 안전장치 및 성능 개선

- **verify_result 타임아웃**: 30초 초과 시 graceful skip (기존: 무한 대기)
- **verify 코드 truncation**: 최대 4000자만 전달 (토큰 폭발 방지)
- **simple 태스크 verify 스킵**: 500자 미만은 검증 불필요
- **execute_code 이중 생성 제거**: generate 태스크에서 FAST 호출 스킵 → STRONG 직행
  - Before: execute(54s, 7132tok) + generate(56s, 3552tok) = 110초
  - After: execute(0s, 스킵) + generate(~15s) = ~15초

### 6. CI/CD green 달성

- ruff lint + format 전체 수정 (F401, F841, 포맷팅)
- 기존 테스트 버그 수정 (스트리밍 노드, 프롬프트, 라우팅)
- CI 전 단계 통과: Lint & Format ✓ | Test ✓ | Docker Build ✓

## 현재 상태

### 커밋 이력 (이번 세션)
```
83c7a3b feat: 컨텍스트 사용량 상태 표시 — 턴 종료 시 프로그레스 바
18bce8a feat: 코드 전문 스트리밍 숨김 — 파일 요약만 표시
5331a18 feat: 4-Tier 모델 체계 도입 — REASONING 티어 추가
f127775 feat: DashScope(Qwen 공식 API) 직접 호출 프로바이더 추가
eaa4a41 fix: 내부 에이전트 raw 출력 숨김 — inclusion list로 전환
b2b7d0b fix: Live Markdown 깜박임 제거 — raw 토큰 스트리밍으로 복원
c565c0d fix: verify_result 안전장치 강화 — 타임아웃/truncation/graceful skip
b639f7b fix: 코드 블록 filepath 파서 강화 — 블록 외부 패턴 지원
9bb5a64 fix: dotfile(.env, .gitignore) filepath 파싱 지원
e72a026 fix: generate 태스크 이중 생성 제거 — execute_code FAST 호출 스킵
86449a3 feat: Planner Agent 도입 + Live Markdown 스트리밍 + 자동 파일 저장
```

### 테스트: 150+ passed (CLI + CodingAssistant), CI green
### DashScope 연결: 4티어 모두 1~1.5초 응답 확인

## 다음 단계 (제안)

1. **DashScope 실환경 테스트** — 실제 `youngs75-agent` 실행하여 Planner + 4-tier + 파일 저장 E2E 검증
2. **Planner 계획 → 페이즈별 실행** — 현재는 계획 전문을 한 번에 전달, 향후 페이즈별 순차 실행 구현
3. **Explore Agent 도입** — Claude Code의 Explore Agent 패턴 (read-only 코드베이스 탐색 전문)
4. **Verification Agent 분리** — verify를 별도 에이전트로 분리 (도구 실행 기반 검증: lint, test)
5. **스트리밍 UX 고도화** — 코드 생성 중 파일별 진행 상태 표시

## 중요 참고사항

- `.env`에 `LLM_PROVIDER=dashscope`, `DASHSCOPE_API_KEY`, `DASHSCOPE_BASE_URL` 설정 완료
- DashScope 워크스페이스: `ap-southeast-1` (싱가포르 리전)
- OpenRouter 설정은 `.env`에 그대로 보존 (전환 가능)
- `fibonacci.py`(test_project에서 생성된 파일)에 구문 오류 있음 — ruff exclude 필요
