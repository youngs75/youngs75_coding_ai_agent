# 세션 인수인계: 2026-04-07 #0002

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/sds/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

> kanban board 4회차 e2e 테스트를 수행하며 발견된 에이전트 프레임워크 버그 6건을 수정.
> Langfuse 트레이싱으로 LLM 호출 패턴을 분석하여 근본 원인을 특정.
> 범용성 검증을 통해 kanban board 특화 수정이 아닌 범용 개선임을 확인.

### 1. jest/vitest 테스트 실행기 추가 (프론트엔드 테스트 프레임워크 통합)
`agents/coding_assistant/agent.py`에 `_run_frontend_tests()` 메서드 추가.

- 테스트 러너 자동 탐지: `npm test` → `vitest` → `jest` (우선순위 기반)
- `package.json`의 기본 "no test specified" 스크립트 무시
- vitest.config / vite.config / jest.config 감지
- pytest 결과와 프론트엔드 테스트 결과 병합
- `_FE_TEST_PATTERN`: `.test.js`, `.spec.ts` 등 프론트엔드 테스트 파일 감지
- `tests/test_run_tests.py`에 `TestRunFrontendTests` 7개 테스트 추가

### 2. `_inject_test_failure` 그래프 연결 (핵심 버그 수정)
**문제**: `_inject_test_failure`가 정의만 되고 그래프에 연결되지 않아, 테스트 실패 시 LLM에 에러 정보가 전달되지 않음 → 동일 코드 무한 반복 생성.

- `NODE_NAMES`에 `INJECT_TEST_FAILURE` 추가
- `init_nodes`에 노드 등록
- `init_edges`에 `INJECT_TEST_FAILURE → GENERATE_FINAL` 엣지 추가
- `_should_retry_tests` 라우팅: `GENERATE_FINAL` → `INJECT_TEST_FAILURE`로 변경

### 3. HITL 환경 승인 multi-phase 호환 (env_approved)
**문제**: multi-phase 실행 시 `interrupt()`가 `GraphInterrupt` 발생 → `except Exception`에 걸려 phase 실패.

- `_execute_phases_sequentially`의 `ainvoke()`에 `env_approved: True` 추가
- 사용자가 계획을 승인한 시점에서 환경 설정도 암묵적 승인

### 4. max_tokens 모델별 설정
**문제**: `max_tokens` 미설정으로 모델 기본값(4096) 적용 → 코드 잘림.

- `core/model_tiers.py`의 `create_chat_model()`에 모델별 `max_tokens` 테이블 추가
- qwen-turbo/plus: 8192, qwen-coder-plus: 16384, 기타: 16384

### 5. 반복 감지 시스템 (동일 코드 + 동일 테스트 에러)
**문제**: 같은 코드/에러를 10회까지 반복하며 시간 낭비.

- **코드 반복 감지**: `_prev_generated_code`와 유사도 비교 (whitespace 정규화 + 길이 5% 이내 + 앞 500자)
- **테스트 에러 반복 감지**: `_prev_test_output`의 FAILED/ERROR 패턴 비교, 연속 3회 동일이면 조기 중단
- 에러 패턴 다국어 확장: pytest, Go(`--- FAIL`, `panic`), Rust(`failures:`, `panicked`), Jest(`FAIL`, `●`) 지원
- `schemas.py`에 `_prev_generated_code`, `_prev_test_output` 필드 추가

### 6. 의존성 재설치 최적화
**문제**: 매 재시도마다 `deps+deps+deps...` 반복 설치 → 시간 낭비.

- `iteration > 0`일 때 의존성 파일(requirements.txt, package.json 등) 변경 감지 시에만 재설치
- 변경 없으면 스킵

### 7. 이전 phase 파일 수정 허용
**문제**: phase 2에서 phase 1의 `__init__.py` 수정 불가 → Blueprint 등록 누락 해결 불가.

- `_inject_test_failure`에서 workspace 전체 파일 목록을 LLM에 제공
- 범용 안내: "에러의 원인이 되는 파일이 있다면 해당 파일도 함께 수정하세요"

### 8. 재시도 횟수 확대
- `max_iterations`: 2 → 11 (최대 10회 재시도)

### 9. 범용성 검증 및 보완
- Blueprint 특화 예시 → 범용 안내문으로 변경
- 테스트 에러 감지 패턴을 Go/Rust/Jest까지 확장
- max_tokens를 프로바이더/모델별로 분기

### 10. tmux 최적화
- `escape-time`: 500ms → 0ms
- `history-limit`: 2000 → 1000
- `~/.tmux.conf`에 영구 저장

## 현재 상태

### 테스트: 881 passed, 43 skipped (외부 API 의존 13 failed)
기존 테스트 영향 없음. 새 테스트 7개(프론트엔드) + 1개(라우팅) 추가.

### kanban board 4회차 결과
- Phase 1: 2~3회 재시도 후 통과 ✅
- Phase 2: 동일 404 에러 반복 (Blueprint 등록 누락) — max iterations 도달 ❌
- Phase 3~4(5~6): 통과 ✅

Phase 2 실패 원인은 Langfuse 트레이싱으로 확인:
- LLM이 "Blueprint 미등록" 정확히 진단하지만, phase 범위 제한으로 `__init__.py` 수정 불가
- `_inject_test_failure`의 파일 목록 힌트가 반영 전 실행이었음

### 변경된 파일 (6개, +408줄)
- `agents/coding_assistant/agent.py` — 핵심 수정 (jest/vitest, inject_test_failure 연결, 반복 감지, deps 최적화)
- `agents/coding_assistant/schemas.py` — `_prev_generated_code`, `_prev_test_output` 필드
- `agents/orchestrator/agent.py` — max_iterations 11, env_approved True
- `core/model_tiers.py` — 모델별 max_tokens 설정
- `tests/test_run_tests.py` — 프론트엔드 테스트 7개 + 라우팅 테스트 업데이트
- `tests/test_fibonacci.py` — 잔여 테스트 수정

## 다음 단계 (제안)

1. **프레임워크 템플릿 MCP + Skills 통합**: Project Creator MCP 도입 + 프레임워크별 Skill 작성 (Flask+Vue, React+Express 등)
   - MCP = 실행 (올바른 보일러플레이트 생성)
   - Skills = 지식 (프레임워크별 규칙/패턴 안내)
   - 참고: [anthropics/skills](https://github.com/anthropics/skills), [SkillsMP](https://skillsmp.com/), [Project Creator MCP](https://glama.ai/mcp/servers/@jackalterman/project-creator-mcp)
2. **kanban board 5회차 테스트**: 이번 수정사항 반영 후 재시도 — 특히 phase 2의 cross-phase 파일 수정 + 반복 감지가 작동하는지
3. **phase 실패 시 결과 표시 개선**: 테스트 실패로 max iterations 도달해도 [OK]로 표시되는 문제 수정
4. **Procedural Memory 연동**: kanban board 성공 패턴을 자동 저장하여 다음 유사 요청 시 참고

## 중요 참고사항

- **`_inject_test_failure` 연결 완료**: 이전 세션에서 미완성이었던 핵심 기능. 테스트 실패 → 에러 분류 → LLM에 수정 지시 → 재생성 경로가 이제 완전히 작동.
- **Langfuse 트레이싱 활용**: 에이전트 디버깅에 매우 효과적. `lf.api.trace.list()` → `lf.api.trace.get()` → GENERATION 분석 패턴 확립.
- **범용성 검증 필수**: 특정 프로젝트(kanban board)에 맞춘 수정이 다른 프로젝트를 깨뜨리지 않는지 항상 확인.
- **tmux 최적화 적용 완료**: `escape-time 0`, `history-limit 1000`. `~/.tmux.conf`에 저장됨.
