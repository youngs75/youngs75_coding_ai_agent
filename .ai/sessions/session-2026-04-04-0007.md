# 세션 인수인계: 2026-04-04 #0007

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/youngs75/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

### Phase 13: 에이전트 고도화 및 테스트 안정화 (3개 작업 병렬 수행)

#### Task 1: SimpleMCPReActAgent create_react_agent 마이그레이션
- `agents/simple_react/agent.py`:
  - import 변경: `langgraph.prebuilt.create_react_agent` → `langchain.agents.create_agent`
  - 파라미터 변경: `prompt` → `system_prompt`
  - 모듈 docstring 업데이트
- langgraph deprecated API를 langchain 1.2.14 최신 API로 마이그레이션 완료

#### Task 2: 메모리 시스템 실제 에이전트 연동 활성화
- `agents/coding_assistant/agent.py`:
  - `RETRIEVE_MEMORY` 노드 추가 (parse_request → **retrieve_memory** → execute_code)
  - `_retrieve_memory()` 비동기 메서드: Procedural Memory 스킬 5건 + Episodic Memory 3건 자동 조회/주입
  - `_record_episodic_memory()` 메서드: 실행 결과(성공/실패)를 Episodic Memory에 자동 기록
  - 그래프 엣지 재배선: parse → retrieve_memory → execute
- `tests/test_coding_assistant.py`:
  - `TestMemoryRetrieval` 5개 테스트 추가
  - `TestEpisodicMemoryAccumulation` 4개 테스트 추가
- 이제 CoALA 4계층 메모리(Working/Episodic/Semantic/Procedural) 중 3계층이 활성 상태

#### Task 3: E2E 테스트 안정화 (flaky 재시도 전략)
- `pyproject.toml`:
  - `pytest-rerunfailures>=14,<17` dev 의존성 추가
  - pytest 설정: `reruns = 2`, `reruns_delay = 3` 전역 재시도
- `tests/conftest.py`: `flaky` 마커 등록
- `tests/test_step2_with_llm.py`: LLM 호출 테스트 2건에 `@pytest.mark.flaky(reruns=3, reruns_delay=5)` 적용
- `tests/test_e2e_phase11.py`: LLM 의존 테스트 4건에 flaky 마커 적용
- `tests/test_docker_e2e.py`: 외부 서비스 테스트 8건에 flaky 마커 적용

#### 추가: CI 기존 린트 실패 수정
- `tests/test_e2e_phase11.py`: import-only 검증 테스트에 `# noqa: F401` 추가 (CI F401 에러 4건 해소)

## 현재 상태
- **Phase 1~13 전체 구현 완료**
- 전체 테스트: **720 passed** (유닛 테스트), 0 failures, 79 deselected (외부 API 의존)
- 신규 테스트: 9개 추가 (메모리 연동 5 + Episodic 축적 4)
- Ruff 린트: 프로젝트 코드 F401 클린
- CI 기존 실패(F401 미사용 import)도 함께 수정
- pytest-rerunfailures 16.1 설치, 전역 재시도 + 개별 flaky 마커 적용

## 다음 단계 후보
1. **Semantic Memory 활성화** — 프로젝트 컨벤션/규칙을 Semantic Memory로 자동 로딩
2. **Prime Jennie 연동 설계** — Self-evolving 코딩 하네스 API 인터페이스 (교육 종료 후)
3. **Langfuse 대시보드 실제 확인** — 클라우드 트레이스 확인, 설정 정합성 검증
4. **DeepResearch 에이전트 메모리 연동** — CodingAssistant 외 다른 에이전트에도 메모리 시스템 확장
5. **E2E 테스트 실제 실행 확인** — MCP 서버 + API 키 환경에서 전체 E2E 검증

## 중요 참고사항
- **모델**: OpenRouter 오픈소스 (STRONG: qwen/qwen3-coder, DEFAULT: deepseek/deepseek-v3.2, FAST: qwen/qwen3.5-9b)
- **패키지 구조**: import는 `youngs75_a2a`. 패키지 변경 후 `uv pip install -e .` 필수.
- **a2a 디렉토리**: `a2a_local/`로 리네이밍됨 (a2a-sdk 충돌 방지)
- **테스트 실행**: `source .venv/bin/activate && python -m pytest tests/ --ignore=tests/eval/ -v`
- **E2E 테스트**: `python -m pytest tests/test_e2e_phase11.py -v` (MCP + API 키 필요)
- **CLI 실행**: `youngs75-agent` (별도 터미널에서)
- **MCP 서버**: `cd docker && docker compose -f docker-compose.mcp.yml up -d`
- **Makefile**: `make help`로 전체 명령어 확인
- **Claude MAX 200 구독** + $200 무료 크레딧 (2026-04-17까지)
- **프로덕션 목표**: Prime Jennie Self-evolving 코딩 하네스
- **LangGraph API**: `context_schema`, `input_schema`, `output_schema` 사용 (V1.1.4 기준)
- **create_agent API**: `langchain.agents.create_agent` 사용 (langgraph.prebuilt deprecated 대체)
- **메모리 시스템**: Procedural(축적+조회), Episodic(기록+조회) 활성, Semantic은 스캐폴딩만
- **ReAct 루프**: `max_tool_calls` 기본 10회, `project_context` 자동 중복 제거
- **Docker harness**: `mcp-code-tools` 서비스 포함, Phase 11 환경변수 와이어링 완료
- **pytest-rerunfailures**: 전역 reruns=2, LLM 의존 테스트는 reruns=3
- **CI/CD**: ruff lint + test + docker build (ci.yml), 배포는 cd.yml
