# 세션 인수인계: 2026-04-03 #0003

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: SDS AX Advanced 교육 Mini PJT — Claude Code급 Coding AI Agent 구현
- **저장소**: /home/youngs75/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

### Phase 6: 에이전트-CLI 통합 및 시스템 연동 (완료)

#### 1. 에이전트 그래프와 CLI 연동
- `cli/app.py` — `_run_agent_turn()`에서 실제 에이전트 그래프 호출 구현
- 에이전트 팩토리 `_create_agent()` — coding_assistant, deep_research, simple_react, orchestrator 지원
- 세션 기반 인스턴스 캐싱 `_get_or_create_agent()`
- `astream` 기반 노드별 진행 상태 표시 (`_NODE_LABELS`)
- 에이전트별 응답 추출 `_extract_response()`
- `cli/session.py` — `get_cached_agent()`, `cache_agent()` 메서드 추가

#### 2. 메모리 시스템 에이전트 통합 (Semantic + Episodic)
- `agents/coding_assistant/schemas.py` — `semantic_context`, `skill_context`, `episodic_log` 필드 추가 (override_reducer)
- `agents/coding_assistant/agent.py` — `_execute_code`에서 3종 메모리를 시스템 프롬프트에 자동 주입
- `cli/app.py` — `_build_input_state()`에서 MemoryStore의 SEMANTIC/EPISODIC 항목을 상태로 전달
- Episodic 메모리: `_save_episodic_memory()` — 턴 완료 후 실행 결과 저장 (세션 스코프, 최대 5개, 200자 제한)
- Agent-as-a-Judge 안전장치: 검증 실패 시 `[주의] failed` 태그

#### 3. Skills 에이전트 통합
- `core/config.py`, `cli/config.py` — `skills_dir` 필드 추가
- `cli/session.py` — `SkillRegistry` 인스턴스 보유
- `cli/app.py` — `_init_skill_registry()`로 초기화, `_build_input_state()`에서 스킬 컨텍스트 전달

#### 4. Orchestrator 에이전트 CLI 연동
- `cli/commands.py` — `AVAILABLE_AGENTS`에 `"orchestrator"` 추가
- `cli/app.py` — `_create_agent()`에 orchestrator 분기, `_NODE_LABELS`에 classify/delegate/respond 추가
- `_extract_response()`에 orchestrator 전용 응답 추출 (agent_response + selected_agent)
- `_build_input_state()`에 orchestrator 전용 필드 (selected_agent, agent_response)

#### 5. 평가 파이프라인 CLI 연동
- `cli/eval_runner.py` (신규) — EvalResult, run_evaluation_async(), load_last_eval_results(), format_eval_summary()
- `cli/commands.py` — `/eval` (평가 실행), `/eval status` (결과 요약), `/eval run` 커맨드 추가
- DeepEval/Langfuse 키 없이도 graceful degradation

#### 6. Docker 통합
- `docker/Dockerfile.cli` (신규) — Python 3.13-slim + uv, youngs75-agent 엔트리포인트
- `docker/docker-compose.yml` — cli 서비스 추가 (stdin_open/tty, MCP 네트워크 연결, depends_on)

#### 7. 기존 테스트 버그 수정
- `tests/test_coding_assistant.py` — `build_graph()` 수동 호출, `await create()` 사용으로 graph=None 해결
- a2a 네이밍 충돌 해결: `a2a/` → `a2a_local/` 리네이밍, `pyproject.toml` 매핑 수정
- `tests/test_docker_e2e.py` — `test_agent_card` → `check_agent_card` 리네이밍 (pytest 수집 에러 수정)

## 현재 상태
- **Phase 1~6 전체 구현 완료**
- 전체 테스트: **170 passed**, 0 failed (eval 테스트 제외 — LLM API 키 필요)
- 기존 테스트 회귀 없음, a2a 네이밍 충돌 완전 해결

## 다음 단계 후보
1. **토큰 스트리밍** — `astream_events`로 토큰 단위 실시간 출력 (현재는 노드 단위)
2. **Procedural 메모리 활성화** — 학습된 스킬 패턴 자동 누적 (Voyager 패턴)
3. **LangGraph Checkpointer 통합** — 멀티턴 대화 상태 유지 (MemorySaver/SQLite)
4. **CLI 확장** — `/skill activate <name>` 커맨드, `/history` 커맨드
5. **Docker E2E 검증** — CLI 컨테이너에서 MCP/Agent 서비스 연동 실제 테스트
6. **프롬프트 최적화** — eval_pipeline Loop 3 (remediation) 연동

## 중요 참고사항
- **패키지 구조**: 디렉토리명 `youngs75_coding_ai_agent`이지만 import는 `youngs75_a2a`로 사용. pyproject.toml의 setuptools package-dir 매핑으로 해결. 패키지 변경 후 `uv pip install -e .` 필수.
- **a2a 디렉토리**: `a2a/` → `a2a_local/`로 리네이밍됨. a2a-sdk 패키지와의 네이밍 충돌 방지. import는 `youngs75_a2a.a2a`로 동일.
- **테스트 실행**: `source .venv/bin/activate && python -m pytest tests/ --ignore=tests/eval/ -v`
- **CLI 실행**: `youngs75-agent` 또는 `python -m youngs75_a2a.cli`
- **Docker CLI**: `cd docker && docker compose run --rm cli`
- 교육 패턴 연결: CoALA(메모리), GAM(검색), Puppeteer(SubAgent), RubricRewards(Generator/Verifier), Agent-as-a-Judge(오류전파방지), DeepAgents CLI(Skills, CLI)
