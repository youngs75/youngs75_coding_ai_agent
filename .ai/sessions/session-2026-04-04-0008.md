# 세션 인수인계: 2026-04-04 #0008

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/youngs75/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

### 1. STRONG 모델 업그레이드 (2회)
- 1차: `qwen/qwen3-coder` → `qwen/qwen3-coder-next` ($0.12/$0.75)
- 2차: 전 티어 Qwen 시리즈 통일
  - STRONG: `qwen/qwen3-coder-plus` ($0.65/$3.25, 1M ctx)
  - DEFAULT: `qwen/qwen3-coder-next` ($0.12/$0.75, 262K ctx)
  - FAST: `qwen/qwen3.5-flash-02-23` ($0.07/$0.26, 1M ctx)
- `core/model_tiers.py`: 비용 DB에 Qwen 모델 6개 + 참조 모델 2개 등록
- 모든 폴백 기본값 업데이트 (config.py, model_tiers.py, deep_research config)

### 2. Semantic Memory 활성화
- `core/memory/semantic_loader.py` 신규 생성 — `SemanticMemoryLoader`
  - AGENTS.md에서 커뮤니케이션/커밋/개발 규칙, 기술스택, 프로젝트 구조 자동 추출 (5개 섹션)
  - pyproject.toml에서 프로젝트 메타/의존성 추출 (2개 항목)
  - `load_all()` 호출 시 기존 semantic 초기화 후 재로딩
- 테스트 7건 추가 (실제 프로젝트 통합 테스트 포함)

### 3. DeepResearch 에이전트 메모리 연동
- `agents/deep_research/schemas.py`: AgentState에 semantic_context, episodic_log 필드 추가
- `agents/deep_research/agent.py`: _retrieve_memory, _record_episodic 노드 추가, 그래프 엣지 재배선
- `agents/deep_research/agent_a2a.py`: 동일 메모리 패턴 적용
- `agents/deep_research/nodes/clarify.py`: 라우팅 → retrieve_memory 경유
- `agents/deep_research/nodes/brief.py`: 프롬프트에 semantic/episodic 컨텍스트 주입
- 테스트 2건 추가

### 4. E2E 테스트 실행 및 시나리오 검증
- Phase 11 E2E 테스트 **18개 전부 통과** (MCP + OpenRouter API 실환경)
- 시나리오 E2E 테스트 5건 작성 (`tests/test_scenarios_e2e.py`)
  - 단순 코드 생성 (도구 최소화) ✅
  - MCP 파일 읽기 (타임아웃 — OpenRouter 도구호출 지연)
  - 버그 수정 (fibonacci n-2 수정) ✅
  - 검증기 품질 (문법 오류 탐지) ✅
  - Semantic Memory 주입 ✅

### 5. 프롬프트 개선 (EXECUTE_SYSTEM_PROMPT)
- 독립적 코드 생성 시 도구 없이 즉시 작성하도록 유도
- 파일 분석 요청 시 반드시 read_file 사용 유도
- 파일 생성 규칙: 사용자 경로 미지정 시 코드만 응답, write_file 호출 금지
- 금지 경로(.claude/, .git/, __pycache__/) 프롬프트에 명시
- task_type에 "analyze" 추가 (파일 분석 요청 분류)

### 6. 보안: 금지 경로 쓰기 차단
- `core/tool_permissions.py`: `_FORBIDDEN_WRITE_PATTERNS` 추가
  - `.claude/`, `.git/`, `__pycache__/`, `node_modules/` 쓰기 → 즉시 DENY
- 테스트로 검증: 4개 금지 경로 DENY, 정상 경로 ALLOW, 민감 파일 ASK

### 7. CLI UX 개선
- `cli/renderer.py` 전면 리뉴얼 (Claude Code UX 패턴 참조)
  - 스피너(애니메이션) 진행 상태 표시
  - 도구 호출 시 `⚡ tool_name path` 표시
  - 검증 결과 `✓ 검증 통과` / `✗ 검증 실패` + 이슈 목록
  - 턴 소요시간 `⏱ 12.3s`
  - 에이전트 초기화 스피너 → 완료 체크마크
  - 성공(✓)/경고(⚠)/에러(✗) 아이콘
- `cli/app.py`: 새 렌더러 API 적용, on_tool_start 이벤트 캡처

### 8. Skills 시스템 활성화
- `data/skills/` 디렉토리에 7개 스킬 YAML 생성
  - code_review, test_generation, refactor, security_review, explain, debug, commit
- Claude Code 스킬 패턴 참조 (simplify, security-review, debug 등)
- `SKILLS_DIR=./data/skills` 환경변수 설정
- CLI 시작 시 L1 자동 발견 → /skill activate로 L2 활성화 → skill_context 프롬프트 주입

### 9. Subagents 시스템 활성화
- `agents/orchestrator/agent.py`: 로컬 에이전트 직접 호출 폴백 추가
  - A2A HTTP 엔드포인트 → 실패 시 → _invoke_local_agent() 폴백
  - coding_assistant, deep_research, simple_react 로컬 호출 지원
- `agents/orchestrator/config.py`: 기본 에이전트 3개 자동 등록
- /agent orchestrator로 전환 후 복합 작업 위임 가능

## 현재 상태
- **Phase 1~14 전체 구현 완료**
- 전체 단위 테스트: 500+ passed, 0 failures
- E2E 테스트: 18 passed (Phase 11)
- Ruff 린트: 전체 클린
- 모델: 전 티어 Qwen 시리즈 (STRONG/DEFAULT/FAST)
- 메모리: CoALA 4계층 전부 활성 (Working/Episodic/Semantic/Procedural)
- Skills: 7개 활성 (code_review, test_generation, refactor, security_review, explain, debug, commit)
- Subagents: Orchestrator → 로컬 에이전트 직접 호출 가능

## 다음 단계 후보
1. **Langfuse 대시보드 실제 확인** — 클라우드 트레이스 확인, 설정 정합성 검증
2. **Prime Jennie 연동 설계** — Self-evolving 코딩 하네스 API 인터페이스
3. **OpenRouter 도구 호출 최적화** — FAST 모델로 도구 호출, STRONG으로 최종 생성하는 2단계
4. **스킬 자동 선택** — parse 결과의 task_type에 따라 관련 스킬 자동 활성화
5. **Orchestrator 실사용 테스트** — 복합 작업 위임 E2E 검증

## 중요 참고사항
- **모델 (Qwen 시리즈)**: STRONG: qwen/qwen3-coder-plus (1M), DEFAULT: qwen/qwen3-coder-next (262K), FAST: qwen/qwen3.5-flash-02-23 (1M)
- **패키지 구조**: import는 `youngs75_a2a`. 패키지 변경 후 `uv pip install -e .` 필수
- **a2a 디렉토리**: `a2a_local/`로 리네이밍됨 (a2a-sdk 충돌 방지)
- **테스트 실행**: `source .venv/bin/activate && python -m pytest tests/ --ignore=tests/eval/ -v`
- **E2E 테스트**: `python -m pytest tests/test_e2e_phase11.py -v` (MCP + API 키 필요)
- **시나리오 테스트**: `python -m pytest tests/test_scenarios_e2e.py -v` (MCP + API 키 필요)
- **CLI 실행**: `youngs75-agent` (별도 터미널에서)
- **MCP 서버**: `cd docker && docker compose -f docker-compose.mcp.yml up -d`
- **Makefile**: `make help`로 전체 명령어 확인
- **Skills**: `/skill list`로 조회, `/skill activate <name>`으로 활성화
- **Orchestrator**: `/agent orchestrator`로 전환, 복합 작업 자동 분해+위임
- **금지 경로**: `.claude/`, `.git/`, `__pycache__/`, `node_modules/` 쓰기 자동 DENY
- **프로덕션 목표**: Prime Jennie Self-evolving 코딩 하네스
- **메모리 시스템**: Procedural(축적+조회), Episodic(기록+조회), Semantic(자동 로딩) 전부 활성
- **ReAct 루프**: `max_tool_calls` 기본 10회, `project_context` 자동 중복 제거
- **Docker harness**: `mcp-code-tools` 서비스 포함, Phase 11 환경변수 와이어링 완료
- **pytest-rerunfailures**: 전역 reruns=2, LLM 의존 테스트는 reruns=3
- **CI/CD**: ruff lint + test + docker build (ci.yml), 배포는 cd.yml
- **UX**: 스피너, 도구 호출 표시, 검증 결과 아이콘, 소요시간 표시
