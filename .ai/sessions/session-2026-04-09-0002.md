# 세션 인수인계: 2026-04-09 #0002

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Coding Agent Harness)
- **목표**: 패키지 구조 정규화 + 코드 생성 품질 강화 (DeepAgents/Codex 패턴 도입)
- **패키지명**: `coding_agent` (실제 디렉토리 구조로 전환 완료)

## 이번 세션 작업 내역

### 1. 패키지 구조 정규화 (가상 매핑 → 실제 디렉토리)
- `pyproject.toml`의 29줄 `package-dir` 매핑 → 3줄 `packages.find` 자동 탐색으로 교체
- 소스 디렉토리 `core/`, `agents/`, `cli/` 등을 `coding_agent/` 하위로 git mv
- `a2a_local/` → `coding_agent/a2a/`로 리네이밍 (import 경로와 디렉토리명 일치)
- Dockerfile 3개: 개별 COPY 8줄 → `COPY coding_agent/ /app/coding_agent/` 1줄로 단순화
- `pyrightconfig.json` 추가 — VS Code/Pylance에서 import 인식 정상화
- docker-compose.yml: `python -m coding_agent.tests.*` → `python tests/*.py` 직접 실행으로 변경
- Makefile: `scripts/` 참조를 `coding_agent/scripts/`로 업데이트
- AGENTS.md 프로젝트 구조 다이어그램 업데이트

### 2. 의존성 재설치 스킵 버그 수정
- **원인**: `_inject_test_failure`에서 `written_files: []`로 초기화 → 다음 iteration의 `_run_tests`에서 의존성 파일 변경 감지 불가
- **수정**: `_deps_changed` 플래그를 CodingState에 추가, `_inject_test_failure`에서 설정, `_run_tests`에서 확인하여 재설치 수행

### 3. ModuleNotFoundError 자동 패치
- `_inject_test_failure`에서 `No module named 'xxx'` 파싱 → requirements.txt에 자동 추가
- 프로젝트 내부 모듈(backend, tests 등)은 자동 추가 대상에서 제외
- `_deps_changed=True` 설정으로 다음 iteration에서 재설치 보장

### 4. OpenRouter 전환
- `.env`의 `LLM_PROVIDER=openrouter`로 변경
- docker-compose.yml: 에이전트 서비스에 `env_file: ../.env` 추가 + `LLM_PROVIDER` 하드코딩 폴백 제거
- **원인**: docker-compose의 `${VAR:-default}` 치환은 compose 파일 디렉토리의 `.env`만 참조하므로, `env_file`로 직접 주입 필요

### 5. Codex식 코드 리뷰 프롬프트 (Verifier 강화)
- VERIFY_SYSTEM_PROMPT: 8단계 검증 기준 + P0-P3 우선순위 태깅
- P0/P1만 실패 판정, P2/P3는 통과하되 issues에 기록
- **프론트-백엔드 일치** 검증 항목 추가 (API 메서드/경로/파라미터)
- **함수 시그니처 일치** 검증 항목 추가

### 6. API 일치 정적 검증 (`_check_api_consistency`)
- 생성된 코드에서 백엔드 라우트(Flask `@api.route`)와 프론트엔드 API 호출(axios)의 HTTP 메서드/경로를 정적 교차 검증
- 불일치 시 P1 이슈로 자동 판정 → Verifier 실패 → 코드 재생성

### 7. 실패 패턴 학습 (`_save_failure_pattern`)
- 최대 반복 도달 시 실패 에러 유형 + 시도한 해결법 + 교훈을 Procedural Memory에 저장
- 태그: `[error_type, language, task_type, "test_failure", "anti_pattern"]`
- 미래 동일 에러 발생 시 에스컬레이션 힌트에서 참조

### 8. 스킬 보강
- **flask_vue.yaml**: Pinia 스토어 패턴, HTML5 네이티브 드래그&드롭 패턴, API 메서드 일치 규칙, 컴포넌트 props/emit 규칙 추가
- **react_express.yaml**: React 드래그&드롭 패턴, API 메서드 일치 규칙 추가
- **fastapi_react.yaml**: API 메서드 불일치, 함수 시그니처 불일치 Common Pitfalls 추가
- **create_prd.yaml** (신규): PRD 생성 스킬 — 요구사항 명확화 + 구조화된 문서 작성
- **generate_tasks.yaml** (신규): 태스크 리스트 생성 스킬 — PRD를 단계별 태스크로 분해

## 현재 상태

### 검증 결과
- **pytest**: 962 passed, 0 failed (eval 제외)
- **스킬**: 13개 등록 (create_prd, generate_tasks 포함)
- **칸반보드 E2E**: 3/3 Phase 완료, 19개 파일 생성, 테스트 통과

### 칸반보드 E2E 실행 비교
| 항목 | 1차 (수정 전) | 2차 (버그 픽스 후) | 3차 (스킬 보강 후) |
|------|-------------|-----------------|-----------------|
| Phase 완료 | 0/4 | 3/3 | 3/3 |
| API 메서드 일치 | 불일치 | 불일치 | 일치 |
| 드래그&드롭 | vue3-smooth-dnd | vue3-smooth-dnd | HTML5 네이티브 |
| 재시도 횟수 | 6회 (최대 도달) | 4회 | 3회 |
| 소요 시간 | 300s (실패) | 564s | 470s |

### LLM 프로바이더
- **현재**: `LLM_PROVIDER=openrouter` (Langfuse 확인 완료)
- REASONING: `openrouter/qwen/qwen3-max`
- STRONG: `openrouter/qwen/qwen3-coder-plus`
- DEFAULT: `openrouter/qwen/qwen3-coder-next`
- FAST: `openrouter/qwen/qwen3.5-flash-02-23`

## 다음 단계

### 1. Langfuse 트레이싱 모델명 수정
- OpenRouter 경유 시 Langfuse에 모델명이 `?`로 표시됨
- LiteLLM의 OpenRouter 모델 리포팅 설정 확인 필요

### 2. 교육 자료 PRD/Task 스킬 실전 테스트
- create_prd, generate_tasks 스킬이 Planner에서 실제 활용되는지 E2E 확인

### 3. 칸반보드 Column.vue 문법 수정
- 파일 끝에 불필요한 `</template>` 태그 중복 — LLM 코드 생성 시 발생

### 4. 파일 경로 정규화
- Phase 간 절대 경로/상대 경로 혼재 문제 (세션 #0005 TODO)

## 중요 참고사항

### 패키지 구조
- 소스 코드는 `coding_agent/` 실제 디렉토리 하위에 통합됨
- `pyproject.toml`의 `packages.find`로 자동 탐색 (수동 매핑 불필요)
- VS Code: `pyrightconfig.json`으로 .venv 인식 설정 완료

### Docker 환경변수 주입
- `docker-compose.yml`의 `${VAR:-default}` 치환은 compose 파일 디렉토리의 `.env`만 참조
- 에이전트 서비스에 `env_file: ../.env`를 추가하여 프로젝트 루트 `.env` 직접 주입
- `environment:` 섹션에서 `LLM_PROVIDER` 폴백 제거 완료

### DashScope → OpenRouter 전환
- DashScope 무료 쿼터 소진으로 OpenRouter 경유 Qwen3 사용
- OPENROUTER_API_KEY: `.env`에 설정됨
