# 세션 인수인계: 2026-04-04 #0006

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/youngs75/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

### Phase 12: 프로덕션 준비 (4개 작업 순차 완료)

#### Task 1: Langfuse 설정 정합성 수정
- `eval_pipeline/settings.py`: `langfuse_base_url` 필드 추가 (LANGFUSE_BASE_URL alias)
- `eval_pipeline/observability/langfuse.py`: `enabled()` 함수에서 `base_url or host` 체크로 수정
- `.env.example`: LANGFUSE_BASE_URL을 primary로, LANGFUSE_HOST를 레거시 주석으로 변경
- `tests/test_langfuse_observability.py`: LANGFUSE_BASE_URL 관련 테스트 3개 추가 (총 46개 통과)

#### Task 3: LangGraph Deprecated API 마이그레이션
- `core/base_agent.py`: `config_schema→context_schema`, `input_state→input_schema`, `output_state→output_schema` 리네이밍
- `agents/deep_research/subgraphs/researcher.py`: 동일 마이그레이션
- `agents/deep_research/subgraphs/supervisor.py`: 동일 마이그레이션
- LangGraph 1.1.4의 3가지 deprecated 경고 제거 확인

#### Task 4: CodingAssistant ReAct 루프 강화
- `agents/coding_assistant/schemas.py`: `tool_call_count` 필드 추가
- `agents/coding_assistant/agent.py`:
  - `max_tool_calls` 한도 체크 (초과 시 skip 메시지 반환)
  - `project_context` 파일 경로 기준 중복 제거 (동일 경로 갱신)
  - `tool_call_count` 누적/리셋 로직
  - `issues` join 시 dict 타입 처리 버그 수정 (기존 버그)
- `tests/test_coding_assistant.py`: Phase 12 테스트 6개 추가

#### Task 5: 프로덕션 배포 준비
- `docker/docker-compose.harness.yml`:
  - `mcp-code-tools` 서비스 추가 (port 3003, 파일 I/O/코드 검색/실행)
  - `agent-coding-assistant`: Phase 11 환경변수 추가 (HOOK_CONFIG_PATH, TOOL_PERMISSIONS_PATH, ENABLE_PARALLEL_TOOLS, COORDINATOR_MODE), MCP 의존성, 헬스체크 강화
  - `orchestrator`: COORDINATOR_MODE 환경변수 추가
- `pyproject.toml`: `langgraph>=1.1.4` 명시적 런타임 의존성 추가
- `.env.example`: Phase 11 설정 변수 섹션 추가

## 현재 상태
- **Phase 1~12 전체 구현 완료**
- 전체 테스트: **705 passed** (기존 + Phase 12 신규 9개), 24 skipped
- 실패: 1건 (기존 deep_research timeout — OpenRouter 지연, flaky)
- Ruff 린트: 기존 untracked 파일 이슈만 (프로젝트 코드 클린)
- 의존성: `pip check` 통과, 충돌 없음
- Docker Compose YAML 검증 통과

## 다음 단계 후보
1. **SimpleMCPReActAgent `create_react_agent` 마이그레이션** — `langgraph.prebuilt.create_react_agent` → `langchain.agents.create_agent` (시그니처 변경: `prompt→system_prompt` 등)
2. **Prime Jennie 연동 설계** — Self-evolving 코딩 하네스 API 인터페이스 (교육 종료 후)
3. **메모리 시스템 활성화** — Episodic/Procedural Memory 실제 에이전트 연동 (현재 구현만 됨)
4. **Langfuse 대시보드 실제 확인** — 클라우드 트레이스 확인, 설정 정합성 검증
5. **E2E 테스트 안정화** — flaky 테스트 재시도 전략 (OpenRouter 지연 대응)

## 중요 참고사항
- **모델**: OpenRouter 오픈소스 (STRONG: qwen/qwen3-coder, DEFAULT: deepseek/deepseek-v3.2, FAST: qwen/qwen3.5-9b)
- **패키지 구조**: import는 `youngs75_a2a`. 패키지 변경 후 `uv pip install -e .` 필수.
- **a2a 디렉토리**: `a2a_local/`로 리네이밍됨 (a2a-sdk 충돌 방지)
- **테스트 실행**: `source .venv/bin/activate && python -m pytest tests/ --ignore=tests/eval/ -v`
- **E2E 테스트**: `python -m pytest tests/test_e2e_phase11.py -v` (MCP + API 키 필요)
- **CLI 실행**: `youngs75-agent` (별도 터미널에서)
- **MCP 서버**: `cd docker && docker compose -f docker-compose.mcp.yml up -d`
- **Makefile**: `make help`로 전체 명령어 확인
- **분석 문서**: `.ai/analysis/claude-code-and-codex-analysis.md` — Claude Code & Codex 비교 분석
- **Claude MAX 200 구독** + $200 무료 크레딧 (2026-04-17까지)
- **프로덕션 목표**: Prime Jennie Self-evolving 코딩 하네스 — 교육 과제 수준이 아닌 프로덕션급
- **.bashrc**: claude 명령어가 tmux 세션 자동 생성/연결하도록 alias 설정됨
- **훅 설정**: `.agent/hooks.yaml`에서 프로젝트별 훅 등록 가능
- **권한 설정**: `.agent/permissions.yaml`에서 프로젝트별 도구 권한 설정 가능
- **CLI 슬래시 명령어**: `/permissions`, `/context`, `/tools` 추가됨
- **LangGraph API**: `context_schema`, `input_schema`, `output_schema` 사용 (V1.1.4 기준)
- **ReAct 루프**: `max_tool_calls` 기본 10회 한도, `project_context` 자동 중복 제거 적용
- **Docker harness**: `mcp-code-tools` 서비스 추가됨, Phase 11 환경변수 와이어링 완료
- **Prime Jennie 연동은 교육 종료 후 진행** (사용자 결정, 2026-04-04)
