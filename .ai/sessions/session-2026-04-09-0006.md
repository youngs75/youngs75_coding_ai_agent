# 세션 인수인계: 2026-04-09 #0006

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Coding Agent Harness)
- **목표**: 칸반보드 E2E 코드 생성 검증 + Harness 품질 개선
- **패키지명**: `coding_agent` (실제 디렉토리 구조)

## 이번 세션 작업 내역

### 1. 칸반보드 E2E 코드 생성 검증 (최우선 목표)
- Orchestrator A2A API(`http://localhost:18080`)로 칸반보드 요청 3회 전송 → 모두 성공
- 단일 HTML 칸반보드: 9/9 기능 검증 통과 (drag&drop, localStorage, responsive 등)
- Flask+Vue3 풀스택 칸반보드: 3 Phase (백엔드→프론트 설정→UI) 모두 완료, 20개 파일 생성
- Langfuse Tracing API로 전체 LLM 호출 흐름 상세 분석 완료

### 2. FAST 모델(qwen3.5-flash) 429 에러 해소
- **근본 원인**: `with_structured_output(method="function_calling")`이 `tool_choice` 전송 → OpenRouter에서 404 → LiteLLM cooldown → 429 폭주
- **수정**: FAST 모델은 `method="json_mode"`로 전환 (agent.py 2곳 + model_tiers.py 범용)
- **추가 수정**: `json_mode` 사용 시 Alibaba 프로바이더가 메시지에 "json" 단어 필수 요구 → MANIFEST 프롬프트에 "응답은 반드시 JSON 형식으로 반환하세요" 추가
- **결과**: 429 에러 8건 → 0건, FAST 모델 에러 7건 → 0건

### 3. HTML 검증 단계 추가 (RUN_TESTS)
- `_run_tests`에 1.2단계 추가: HTML 파싱 오류, 필수 태그(<html>, <head>, <body>), 임베디드 JS `node --check` 검증
- `_validate_html_files()` 메서드 구현 (약 130줄)
- HTML 파일이 있으면 node 런타임도 감지하도록 `needed_langs` 로직 수정
- `frontend_only` 플래그에 html_files 포함

### 4. VERIFY 스킵 로직 개선
- 기존: `task_type == "generate" and len(code) < 500` → 무조건 스킵
- 개선: HTML/프론트엔드 코드(html, css, javascript, vue, react 등) 감지 시 항상 검증

### 5. Docker 메모리 시스템 연결 (구조 완료, 재빌드 필요)
- **발견된 문제**: Docker 환경에서 MemoryStore가 전혀 전달되지 않아 메모리 기능 비활성화 상태
- `docker-compose.yml`: `memory_data` 볼륨 추가, orchestrator + agent-coding에 마운트
- `run_coding_assistant_server.py`: MemoryStore 생성 → CodingAssistantAgent에 전달
- `run_orchestrator_server.py`: MemoryStore 생성 → OrchestratorAgent에 전달
- `orchestrator/agent.py`: `__init__`에 `memory_store` 파라미터 추가, 전역 `_memory_store` 설정, SubAgent spawn 시 전달

### 6. LiteLLM 설정 수정
- `docker/litellm_config.yaml`: qwen3.5-flash 모델에 `drop_params: true` 추가

## 현재 상태

### 검증 결과
- **pytest**: 995 passed, 4 skipped (회귀 없음)
- **Docker E2E**: 칸반보드 단일 HTML 3회 성공, Flask+Vue3 2회 성공
- **Langfuse**: 최종 실행 시 에러 0건 (50개 generation 전부 성공)

### LLM 프로바이더
- `LLM_PROVIDER=openrouter` (OpenRouter 경유 Qwen3 시리즈)
- REASONING: qwen3-max, STRONG: qwen3-coder-plus, DEFAULT: qwen3-coder-next, FAST: qwen3.5-flash

## 다음 단계 (TODO)

### 1. Docker 재빌드 후 메모리 동작 E2E 검증 (최우선)
- `make down && make up` 후 칸반보드 요청
- Langfuse에서 "메모리 컨텍스트" 주입 확인
- Docker 로그에서 "메모리" 초기화 메시지 확인
- procedural.jsonl에 코드 패턴이 자동 누적되는지 확인

### 2. MemoryMiddleware `slm_invoker` 배선 (Critical Gap)
- 현재 `slm_invoker=None`으로 전달되어 domain_knowledge/user_profile 자동 축적이 미동작
- CodingAssistantAgent 미들웨어 체인에서 FAST 모델을 slm_invoker로 전달해야 함
- 요구사항 3-1 "사용자가 입력하는 지식이 누적되는 구조" 충족에 필수

### 3. 칸반보드 산출물 export 및 실행 검증
- CLI에서 `/export` 또는 `--export`로 생성된 파일 추출
- Flask 백엔드 실행 + Vue3 프론트엔드 빌드 → 실제 브라우저 동작 확인

### 4. 요구사항 3개 축 필수 사항 최종 점검
- 축 1 (메모리): slm_invoker 배선 후 domain_knowledge 자동 축적 E2E 검증
- 축 2 (SubAgent): 현재 충족 상태, 추가 작업 불필요
- 축 3 (복원력): 현재 충족 상태 (5/7 완전 구현), 추가 작업 불필요

## 중요 참고사항

### 수정된 파일 목록
- `coding_agent/agents/coding_assistant/agent.py` — HTML 검증, VERIFY 스킵, json_mode 전환
- `coding_agent/core/model_tiers.py` — flash/turbo 모델 json_mode 자동 분기
- `coding_agent/agents/orchestrator/agent.py` — memory_store 전역 + SubAgent 전달
- `docker/docker-compose.yml` — memory_data 볼륨 추가
- `docker/litellm_config.yaml` — drop_params 추가
- `tests/run_coding_assistant_server.py` — MemoryStore 생성/전달
- `tests/run_orchestrator_server.py` — MemoryStore 생성/전달

### Langfuse Tracing API
```bash
export LANGFUSE_AUTH="pk-lf-fe9f8d31-238a-405a-85fe-66ca81c07091:sk-lf-885abaf5-f590-4908-a2a9-e7fabbda792a"
curl -s -u "$LANGFUSE_AUTH" \
  "https://cloud.langfuse.com/api/public/observations?limit=50&type=GENERATION&orderBy=startTime.desc"
```

### 테스트 실행 (변경 파일 관련만)
```bash
source .venv/bin/activate && python -m pytest tests/test_coding_assistant.py -x -q --tb=short
```

### Docker 재빌드
```bash
make down && make up
```
