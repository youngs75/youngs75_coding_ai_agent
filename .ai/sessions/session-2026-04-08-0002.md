# 세션 인수인계: 2026-04-08 #0002

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: 샌드박스 아키텍처 전환 + 코드 생성 파이프라인 안정성 대폭 강화
- **저장소**: /home/sds/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

> 칸반보드 E2E 테스트를 반복 실행하면서 발견된 다수의 버그를 수정하고, 샌드박스 아키텍처로 전환

### 1. 사용자 친화적 CLI 진입점 (`youngs75-agent.sh`)
- 프로젝트 루트에 `youngs75-agent.sh` 생성 (Docker 샌드박스 모드)
- `--export` 옵션: 컨테이너 워크스페이스에서 호스트로 결과물 추출
- `--agent NAME` 옵션: 기본 에이전트 지정
- `Makefile`에 `cli` 타겟 추가
- `/export` CLI 슬래시 커맨드 추가 (`cli/commands.py`)

### 2. 샌드박스 아키텍처 전환
- **Dockerfile.agent**: Python 3.13 + Node.js 22 + Java 21 멀티 런타임
- **docker-compose.harness.yml**: 공유 `workspace_data` 볼륨 (agent ↔ MCP), 호스트 마운트 제거
- `/workspace` 디렉토리가 에이전트 작업 공간 (격리)
- 완료 후 `--export`로 호스트에 결과물 추출

### 3. MCP 서버 동적 Workspace
- `set_workspace()`, `get_workspace()` MCP 도구 추가
- `MCP_MOUNT_ROOT` 환경변수로 허용 범위 제한

### 4. 4-Tier 모델 시스템 버그 수정 (★ 핵심)
- **`core/config.py` `get_model()` 근본 버그 수정**
  - `_resolve_model_name()`이 항상 `self.default_model`(qwen-plus) 반환 → purpose_tiers/model_tiers를 완전히 무시
  - 수정: `get_model()`이 `get_tier_config()`를 사용하도록 변경
  - 결과: generation=qwen3-coder-next(STRONG), verification=qwen3.5-plus(DEFAULT), parsing=qwen3.5-flash(FAST) 정상 적용
- **`core/model_tiers.py` max_tokens 확대**: 8192 → 65536 (모델 실제 지원 한도)

### 5. 코드 생성 → 파일 저장 파이프라인 안정성 강화
- **`_extract_code_blocks` 확장**:
  - filepath 검색 범위: 3줄/200자 → 8줄/500자
  - 백틱 감싼 경로 패턴 추가 (`` `frontend/package.json` ``)
  - `write_file(path=..., content=...)` MCP 도구 호출 형태 파싱 추가
  - filepath 키워드 있으면 확장자 없는 파일(Dockerfile 등)도 매칭
- **`_apply_code` 방어 로직**: 코드 블록 N개 발견 + filepath 0개 → `verify_result.passed=False` 반환하여 자동 재시도
- **프롬프트 강화**: filepath 필수 경고, JSON용 볼드 filepath 안내, 검증에 filepath 누락 감지 항목 추가

### 6. 프론트엔드 테스트 실행 개선
- `_run_fe_tests()`: `node_modules` 없으면 `npm install` 자동 실행
- "Could not find a config file" 에러 감지 → 테스트 도구 미설치로 스킵 처리 (무한 재시도 방지)

### 7. 기타 수정
- LiteLLM Proxy: `api_base` 설정 추가 (DashScope 인증 실패 수정)
- LiteLLM Proxy: Langfuse 키 전달 수정 (environment에서 중복 덮어쓰기 제거)
- LiteLLM Proxy: healthcheck를 `liveliness` 엔드포인트로 변경
- MCP stale 참조 수정: planner/deep_research/simple_react config에서 tavily/serper/arxiv → code_tools 통합
- CLI `_extract_node_summary`: 테스트 실패 시 에러 메시지 표시 개선
- `.dockerignore`: `data/skills/` 예외 처리 추가

## 현재 상태

### Docker Harness (5컨테이너, 모두 healthy)
```
litellm-db       — PostgreSQL 16
litellm-proxy    (:4000) — LLM Gateway + Langfuse
mcp-server       (:3003) — 통합 MCP (17개 도구, set_workspace 포함)
agent-coding     (:18084) — CodingAssistant (Python+Node+Java 샌드박스)
orchestrator     (:18080) — Orchestrator
```

### 4-Tier 모델 시스템 (수정 후 정상 동작)
```
REASONING : dashscope/qwen3-max       (계획/아키텍처)
STRONG    : dashscope/qwen3-coder-next (코드 생성, max 65536)
DEFAULT   : dashscope/qwen3.5-plus     (검증/분석)
FAST      : dashscope/qwen3.5-flash    (파싱/분류)
```

### 칸반보드 테스트 결과 (최종)
- Phase 1 (백엔드): ✅ 10파일, 테스트 통과
- Phase 2 (프론트엔드 설정): 최신 실행 결과 확인 필요
- Phase 3 (UI 컴포넌트): 최신 실행 결과 확인 필요
- Export 기능: ✅ 정상 동작

### 변경된 파일 (19개)
1. `youngs75-agent.sh` — 신규, 샌드박스 CLI 진입점
2. `core/config.py` — ★ get_model() 4-Tier 버그 수정
3. `core/model_tiers.py` — max_tokens 65536 확대
4. `agents/coding_assistant/agent.py` — filepath 추출 확장 + apply_code 방어 + FE 테스트 개선
5. `agents/coding_assistant/prompts.py` — filepath 필수 강화 + 검증 항목 추가
6. `agents/planner/config.py` — MCP 통합 서버 참조
7. `agents/deep_research/config.py` — MCP 통합 서버 참조
8. `agents/simple_react/config.py` — MCP 통합 서버 참조
9. `cli/app.py` — MCP workspace 코드 단순화
10. `cli/commands.py` — /export 커맨드 추가
11. `cli/renderer.py` — welcome 배너 workspace 표시
12. `docker/Dockerfile.agent` — Node.js 22 + Java 21 추가
13. `docker/docker-compose.harness.yml` — 샌드박스 아키텍처 (공유 볼륨)
14. `docker/litellm_config.yaml` — DashScope api_base 추가
15. `mcp_servers/code_tools/server.py` — set_workspace/get_workspace 도구
16. `.dockerignore` — data/skills/ 예외
17. `.env.example` — MCP_HOST_ROOT, HOST_UID/GID 추가
18. `Makefile` — cli 타겟 추가
19. `tests/test_model_tiers.py` — 레거시 테스트 수정

## 다음 단계 (제안)

1. **칸반보드 최종 테스트**: max_tokens 65536 + 4-Tier 정상화 후 전체 Phase 통과 확인
2. **PMS 개발 E2E**: Docker 샌드박스에서 PMS 프롬프트 실행
3. **사용자 성향 자동 학습**: `accumulate_user_profile()` API는 존재하나 에이전트가 자동 호출하는 로직 미구현
4. **A2A 서버 기능 향상**: CLI 대비 HITL, 메모리, 파일 저장 등 기능 격차 해소

## 중요 참고사항

- **4-Tier 버그**: `get_model()`이 `_resolve_model_name()` → `default_model`(qwen-plus)만 사용하던 것을 `get_tier_config()` 경유로 수정. LiteLLM 도입 이후 모든 LLM 호출이 레거시 qwen-plus로 고정되어 있었음
- **샌드박스 모드**: `youngs75-agent.sh`로 CLI 접속 → Docker 내부 `/workspace`에서 작업 → `--export`로 호스트 추출
- **Langfuse**: LiteLLM Proxy 레벨에서 자동 트레이싱 정상 동작. 모델명/토큰/비용 기록됨
- **MCP 통합**: 17개 도구 (코드 11 + 검색 4 + workspace 2)가 단일 서버(3003)에서 제공
