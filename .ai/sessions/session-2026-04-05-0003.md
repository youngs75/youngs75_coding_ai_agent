# 세션 인수인계: 2026-04-05 #0003

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/youngs75/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

### 1. 로컬 Langfuse Docker 관련 파일 정리
클라우드 Langfuse를 사용하므로 로컬 Docker Langfuse 관련 파일/문서 제거:
- **삭제**: docker-compose.langfuse.yaml, .env.langfuse.example, setup_langfuse.py, langfuse_demo.py, LANGFUSE_GUIDE.md, test_langfuse_demo_trace.py
- **수정**: docker-compose.harness.yml (Langfuse 인프라 6개 서비스+볼륨 제거), AGENTS.md, README.md, Makefile (langfuse-up/down 제거), .env.example (클라우드 URL로 변경)

### 2. `make setup` 원커맨드 초기 설정 구현
Claude Code/Codex의 간단한 설치 패턴을 참고하여 `scripts/setup.sh` 작성:
- **Step 1**: uv 패키지 매니저 확인/설치
- **Step 2**: Python 3.13 확인 (없으면 `uv python install 3.13`)
- **Step 3**: 의존성 설치 (`uv sync --frozen`)
- **Step 4**: .env 파일 생성 + 필수 API 키(OPENROUTER_API_KEY) 안내
- **Step 5**: Docker MCP 서버 기동 (대화형 [Y/n], 비대화형 자동 수락)
- **오래된 컨테이너 감지**: 소스가 이미지보다 새로우면 재빌드 제안
- Makefile에 `setup`, `mcp-up`, `mcp-down` 타겟 추가
- .env.example 필수/선택/고급 3단계 구분으로 개선
- README.md 빠른 시작을 `make setup` + `youngs75-agent` 2줄로 축소

### 3. CodingAssistant 무한루프 버그 수정
사용자가 다른 디렉토리에서 CLI 실행 시 `list_directory .` 반복 호출로 recursion_limit 25 초과:
- **원인 1**: `_should_use_tools()`에서 `tool_calls` 체크가 `max_tool_calls` 한도보다 먼저 → 루프 탈출 불가
- **원인 2**: LangGraph recursion_limit 기본값 25가 너무 낮음
- **수정**: `_should_use_tools()`에 `tool_call_count >= max_calls` 선행 체크 추가, CLI에서 recursion_limit=50 설정

## 현재 상태
- Phase 1~15 전체 구현 완료
- 테스트: 734 passed (flaky E2E 1개 제외), 0 failures
- `make setup` 원커맨드 초기 설정 동작 확인
- CodingAssistant 무한루프 수정 완료 (150 passed)

## 다음 단계

### 우선 — CLI 스트리밍 렌더링 개선
현재 CLI는 에이전트 응답 전체를 반복 출력하는 것처럼 보임 (같은 내용이 여러 번 표시).
Claude Code(Ink/React TUI)와 Codex(Rust TUI)는 새 토큰만 마지막에 append하여 실시간 스트리밍.

**조사 필요 사항:**
- `cli/app.py` `_run_agent_turn()`의 `astream_events` 처리 방식 분석
- `cli/renderer.py` CLIRenderer의 토큰 스트리밍 출력 방식 분석
- Claude Code 소스(`/home/youngs75/projects/claude-code-haha`)의 Ink 기반 TUI 렌더링 패턴 참고
- Codex 소스(`/home/youngs75/projects/codex`)의 TUI 렌더링 패턴 참고
- 특히 노드 전환 시 "이전 출력 다시 표시" 문제의 원인 파악
- Rich Live/Console 기반 incremental append 방식으로 전환 검토

### 기타
1. **`sudo rm -rf temp/`** 실행 (root 소유 파일 삭제)
2. **`.cli_history`를 `.gitignore`에 추가**
3. **Orchestrator 실사용 E2E 테스트** — 복합 작업 coordinate 검증
4. **MCP workspace 매핑 검증** — CLI 실행 위치와 Docker MCP 서버 workspace 정합성
5. **스킬 확장** — architecture_review, performance_review 등
6. **A2A 엔드포인트 활성화** — 실제 HTTP 위임 테스트

## 중요 참고사항
- **모델 (Qwen 시리즈)**: STRONG: qwen/qwen3-coder-plus (1M, 180s), DEFAULT: qwen/qwen3-coder-next (262K, 120s), FAST: qwen/qwen3.5-flash-02-23 (1M, 60s)
- **기본 에이전트**: orchestrator (모든 요청 자동 라우팅)
- **패키지 구조**: import는 `youngs75_a2a`. 패키지 변경 후 `uv pip install -e .` 필수
- **테스트 실행**: `source .venv/bin/activate && python -m pytest tests/ --ignore=tests/eval/ -v`
- **CLI 실행**: `youngs75-agent` (가상환경 활성화 필요) 또는 `uv run youngs75-agent`
- **초기 설정**: `make setup` (원커맨드)
