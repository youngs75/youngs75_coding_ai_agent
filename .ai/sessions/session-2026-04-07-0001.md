# 세션 인수인계: 2026-04-07 #0001

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/sds/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

> 이전 세션(#0002, 2026-04-06)의 "다음 단계 제안 5건"을 모두 구현 완료.
> 서브에이전트 4개 병렬 실행 시도 → 파일 충돌 사고 발생 → 복구 후 직접 마무리.

### 1. 에러 메시지 분석 강화 (태스크 1)
`agents/coding_assistant/agent.py`에 에러 유형 분류 시스템 추가.

- `_ERROR_PATTERNS`: 7종 에러 패턴 정의 (CircularImport, ModuleNotFoundError, ImportError, SyntaxError, NameError, AttributeError, TypeError)
- `_classify_test_error()`: pytest 출력에서 정규식으로 에러 유형 분류 → 수정 지시 반환
- `_inject_test_failure()` 개선: 에러 유형 + 수정 방향을 LLM에게 명시적으로 전달
- 예: CircularImport → "공유 객체를 extensions.py로 분리, Factory 패턴 사용"

### 2. Planner Phase 분배 규칙 강화 (태스크 2)
서브에이전트(agent-planner-rules)가 수행.

- `agents/planner/prompts.py`: PLAN_SYSTEM_PROMPT 전면 강화
  - Phase당 최대 5파일 하드 리밋 (절대 규칙)
  - 의존성 순서 강제: 기반 모듈 → 비즈니스 로직 → API → 프론트엔드 → 통합 테스트
  - 파일 간 의존성 그래프 고려: 같은 phase 내 상호 import 금지
  - 자동 검증 힌트 권장
- `agents/planner/schemas.py`: `MAX_FILES_PER_PHASE=5` 상수 + `validate_phase_file_limit()` 자동 분할 + `validate_task_plan()` 통합 검증
- `agents/planner/agent.py`: TaskPlan 구성 직후 `validate_task_plan()` 호출

### 3. 순환 Import 방지 패턴 가이드 (태스크 3)
`agents/coding_assistant/prompts.py`의 EXECUTE_SYSTEM_PROMPT에 신규 섹션 추가.

- Python: extensions.py 분리, Factory 패턴, Blueprint는 app 직접 import 금지
- JavaScript/TypeScript: barrel export 순환 re-export 주의
- Go: interface를 상위 패키지에 정의
- 일반 원칙: import 그래프 DAG 검증 후 코드 작성

### 4. HITL 환경 승인 interrupt (태스크 4)
venv 생성/의존성 설치 전 사용자 확인 메커니즘 추가.

- `agents/coding_assistant/agent.py`: `_run_tests`에서 `interrupt(env_info)` 호출
  - env_info에 workspace, venv 경로, 감지된 런타임, 의존성 목록 포함
  - 기존 venv 존재 시 승인 스킵 (재사용)
  - 거부 시 실행 검증 스킵 → LLM 검증만 진행
  - RuntimeError catch로 테스트 환경에서는 자동 승인
- `agents/coding_assistant/schemas.py`: `env_approved` 필드 추가
- `cli/app.py`: interrupt 타입 구분 (env_approval vs 계획 승인)
- `cli/renderer.py`: `show_env_approval()`, `ask_env_approval()` UI 함수

### 5. 프론트엔드 거짓 통과 방지 (태스크 1과 함께)
`_run_tests`에 JS/TS/Vue 문법 검증 단계(1.5단계) 추가.

- JS 파일: `node --check` 문법 검증
- TS 파일: `npx tsc --noEmit` (tsconfig.json 존재 시)
- 프론트엔드 전용 프로젝트: "프론트엔드 문법 검증 통과" 메시지로 구분

### 6. 서브에이전트 사고 복구
- `agent-hitl-env`가 Write 도구로 `agent.py` 1441줄 → 52줄로 파괴
- `git checkout HEAD --`로 즉시 복구
- 백그라운드 pytest가 프로젝트 루트에 잔여 파일 생성 (fibonacci.py, binary_search.py 등) → 정리 완료

## 현재 상태

### 테스트: 802 passed, 43 skipped, 0 failed
(외부 API/Docker 의존 테스트 제외 기준)

### 변경된 파일 (10개, +372줄)
- `agents/coding_assistant/agent.py` — 에러 분류 + 프론트엔드 검증 + HITL 환경 승인
- `agents/coding_assistant/prompts.py` — 순환 import 방지 가이드
- `agents/coding_assistant/schemas.py` — env_approved 필드
- `agents/planner/__init__.py` — validate_task_plan, MAX_FILES_PER_PHASE export
- `agents/planner/agent.py` — validate_task_plan 호출 통합
- `agents/planner/prompts.py` — Phase 분배 규칙 전면 강화
- `agents/planner/schemas.py` — 파일 수 하드리밋 검증 + 자동 분할
- `cli/app.py` — 환경 승인 interrupt 타입 구분 처리
- `cli/renderer.py` — 환경 승인 UI (show_env_approval, ask_env_approval)
- `tests/test_run_tests.py` — venv 생성을 위한 requirements.txt fixture 추가

## 다음 단계 (제안)

1. **kanban board 4회차 테스트**: 이번 개선사항(에러 분류, Phase 분배, 순환 import 방지)이 실제로 순환 import 문제를 해결하는지 검증
2. **에이전트 팀(TeamCreate) 활용**: 이번 세션에서 서브에이전트 파일 충돌을 경험. 다음부터 병렬 작업은 TeamCreate + TMUX로 격리 실행
3. **Planner 자동 분할 테스트**: validate_phase_file_limit()이 실제 LLM 출력에서 정상 동작하는지 e2e 검증
4. **프론트엔드 테스트 프레임워크 통합**: jest/vitest 실행 지원 추가 (현재 node --check만)
5. **메트릭 수집**: 에러 유형별 수정 성공률 추적 (어떤 에러 유형이 LLM이 잘 고치고, 어떤 것이 못 고치는지)

## 중요 참고사항

- **서브에이전트 파일 충돌 교훈**: Agent 도구의 서브에이전트는 동일 워크스페이스를 공유하므로 Write 도구로 대형 파일을 덮어쓸 위험이 있음. "에이전트 팀" 요청 시 반드시 TeamCreate 사용 (메모리에 저장됨).
- **백그라운드 pytest 주의**: 백그라운드로 pytest를 여러 번 실행하면 테스트 중 생성되는 임시 파일이 프로젝트에 잔류할 수 있음.
- **pytest-timeout 미설치**: `--timeout` 플래그 사용 불가. 필요 시 `pip install pytest-timeout`.
- Claude MAX $200 구독 + $200 크레딧 상태. TMUX 에이전트 팀 설정 완료.
