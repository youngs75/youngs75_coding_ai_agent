# 세션 인수인계: 2026-04-09 #0001

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Coding Agent Harness)
- **목표**: planned vs written 파일 검증 + 패키지 리네이밍 + Docker/코드 구조 대규모 정리
- **패키지명**: `coding_agent` (구 `youngs75_a2a`에서 변경)

## 이번 세션 작업 내역

### 1. Planned vs Written 파일 검증 (TODO 1 + TODO 2)
DeepAgents 오픈소스 참고하여 Planner가 지정한 파일 목록과 실제 생성 파일을 비교하는 이중 검증 도입.

- **`agents/coding_assistant/schemas.py`**: `planned_files: list[str]` 필드 추가
- **`agents/orchestrator/agent.py`**: `build_init_state()`에 `extra_state={"planned_files": phase.files}` 전달
- **`core/subagent_context.py`**: `build_phase_task_message()`에 "생성 필수 파일 체크리스트" 섹션 추가
- **`agents/coding_assistant/prompts.py`**: `GENERATE_FINAL_SYSTEM_PROMPT`에 계획 파일 전수 생성 규칙 추가
- **`agents/coding_assistant/agent.py`**:
  - `_generate_final()`: planned_files를 시스템 프롬프트에 주입
  - `_run_tests()`: planned vs written 비교 → 누락 시 `test_passed=False`

### 2. 기존 테스트 실패 수정 (4건)
- **프론트엔드 테스트 3건** (`test_vitest_config_detected` 등): npm install 실패 시 runner_label 로그가 기록되지 않는 문제 → runner 감지 직후 로그 추가
- **E2E 테스트 1건** (`test_read_and_analyze_file`):
  - `analyze` 태스크가 FAST 모델에서 도구 미호출 → STRONG 직행으로 변경
  - `_verify_result`에서 `tool_call_count: 0` 리셋 제거
  - `_generate_final`에서 도구 호출 카운트를 state에 반영

### 3. Docker 구조 정리
- **MCP 서버 통합**: 분리된 3개(tavily/serper/arxiv) → 통합 1개(`mcp-server:3003`)
- **docker-compose.yml**: 구버전 삭제, `docker-compose.harness.yml` → `docker-compose.yml`로 승격
- **Makefile**: `make up/down/ps` = harness 직접 실행으로 단순화
- **Dockerfile.mcp**: 삭제된 MCP 런처 COPY 제거
- **Dockerfile.agent**: 기본 CMD를 `coding_agent.tests.run_coding_assistant_server`로 변경
- **에이전트 서버**: DashScope 사용 시 `DASHSCOPE_API_KEY` 체크 분기 추가

### 4. 패키지명 리네이밍 (`youngs75_a2a` → `coding_agent`)
- 154개 파일, 941개소 일괄 치환
- pyproject.toml 패키지 매핑, entry point, Docker CMD 모두 반영
- editable install 재설치 + import 검증 완료

### 5. 불필요한 파일 제거 (17개)
- 구버전 서버 런처: `run_agent_server.py`, `run_simple_react_server.py`, `run_deep_research_a2a_server.py`
- 개별 MCP 런처: `run_tavily_mcp.py`, `run_serper_mcp.py`, `run_arxiv_mcp.py`
- 데모/에이전트: `demo_client.py`, `demo_interactive.py`, `echo_agent.py`
- Docker: `docker-compose.yml`(구버전), `docker-compose.mcp.yml`, `Dockerfile.cli`
- 설정: `config/settings.{dev,staging,prod}.env`, `docker/mcp_docker.{ps1,sh}`, `.env.bak`, `.env.backup`

### 6. 테스트 업데이트
- `test_docker_e2e.py`: 구버전 docker-compose 검증 → Harness 구조 검증으로 재작성
- `agents/__init__.py`: EchoAgent import 제거

## 현재 상태

### 검증 결과
- **pytest**: 962 passed, 0 failed (eval 제외)
- **Docker Harness**: 5개 서비스 모두 Healthy
  - litellm-db (:5432), litellm-proxy (:4000), mcp-server (:3003)
  - agent-coding-assistant (:18084), orchestrator (:18080)

### Harness 아키텍처 (최종)
```
Orchestrator (:18080)
  └── CodingAssistant (:18084)
        ├── LiteLLM Proxy (:4000) → DashScope (4-Tier 모델)
        └── MCP Server (:3003) → 통합 도구 (코드 + 검색)
```

## 다음 단계

### 1. CLI E2E 재검증
- 칸반보드 등 멀티 Phase 태스크로 planned vs written 검증이 실제 동작하는지 확인
- 테스트 파일 미생성 시 재생성 요청이 잘 되는지 확인

### 2. 파일 경로 정규화
- Phase 간 절대 경로/상대 경로 혼재 문제 (세션 #0005 TODO 3)

### 3. conftest.py 자동 생성
- flask_vue 스킬에 conftest.py 패턴이 있지만 LLM이 활용하지 않는 문제

## 중요 참고사항

### 패키지명 변경
- 구 패키지명 `youngs75_a2a`는 `.ai/sessions/` 과거 세션 파일에만 잔존
- 모든 Python/Docker/설정 파일은 `coding_agent`로 통일됨
- `a2a_local/` 디렉토리는 A2A SDK 네이밍 충돌 방지용으로 유지 (활성 코드)

### Docker 구조
- `make up` = Harness 전체 기동 (구 `make up-harness`)
- `docker-compose.yml` = 유일한 compose 파일 (Harness 통합 구성)
- 구버전 분리 MCP/Agent 서비스는 완전 제거됨
