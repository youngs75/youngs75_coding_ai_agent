# 세션 인수인계: 2026-04-05 #0008

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/youngs75/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

> 이전 세션(#0007)에서 이어서 추가 작업 수행. #0007의 HITL/CLI UX 구현 후 실환경 테스트 → 버그 수정 → 문서 최신화 → 논문 구현 문서화까지 진행.

### 1. DashScope API URL 수정
- `.env`의 `DASHSCOPE_BASE_URL` 경로 확인 및 복원 (`/compatible-mode/v1`)
- `core/model_tiers.py` fallback URL을 국제판 범용 엔드포인트로 변경 (`dashscope-intl.aliyuncs.com`)
- curl로 워크스페이스/국제판 엔드포인트 모두 HTTP 200 확인

### 2. CLI UX 개선 — 노드 이력 누적 + 결과 요약 + 코드 블록 제거
| 항목 | Before | After |
|------|--------|-------|
| 노드 이력 | transient 스피너 (덮어쓰기) | `✓ 노드명 — 요약` 영구 누적 표시 |
| 노드 요약 | 타이틀만 표시 | classify → `→ coding_assistant`, generate → `353 lines 생성` 등 |
| 최종 응답 | 전체 코드 블록 포함 | 코드 블록 + filepath 헤더 + 중복 파일목록 제거 |

### 3. Human-in-the-loop (HITL) 계획 승인
- LangGraph `interrupt()` + `aget_state()` 기반 구현
- **핵심 발견**: `astream_events`는 `GraphInterrupt`를 exception으로 전파하지 않음 → `aget_state()` 기반으로 pending interrupt를 감지하는 방식 채택
- 흐름: `classify → plan → [interrupt] → 📋 계획 Panel 표시 → 승인 프롬프트 → Command(resume=True) 재개`
- 거부 시 실행 중단 후 프롬프트 복귀

### 4. Classify 프롬프트 개선
- "코드 생성/개발 요청은 항상 coding_assistant" 규칙 추가
- coordinator 오분류 방지 (프론트엔드+백엔드 = 코딩 작업)
- coding_assistant description: "풀스택, 멀티파일 프로젝트 포함" 명시

### 5. 문서 + 구성도 + setup 최신화
- `AGENTS.md`: HITL 패턴 설명, 4-Tier 모델 체계 표 추가
- `docs/ARCHITECTURE.md`: Mermaid 다이어그램 4곳 업데이트 (시스템 구성도, 요청 흐름 시퀀스, 모델 해석 체계, 클래스 다이어그램에 Planner/HITL/4-Tier 반영)
- `docs/USER_GUIDE.md`: 환경변수 예시 DashScope/Qwen으로 교체
- `docs/API.md`: ModelTier에 REASONING 추가, provider 기본값 dashscope
- `docs/architecture_diagram.svg`: 신규 커밋 (이미 최신 상태)
- `scripts/setup.sh`: DashScope API 키 체크 로직 추가 (LLM_PROVIDER 분기)

### 6. Langfuse 트레이싱 검증
- Langfuse API로 최근 세션 트레이스 조회
- HITL 패턴 정상 반영: 동일 세션에 INITIAL + RESUME 2개 트레이스 기록
- 에이전트 체이닝 (Orchestrator → Planner → CodingAssistant) 관측 확인
- 총 7회 LLM 호출, 8,567 토큰 사용 확인

### 7. 7개 논문 구현 매핑 문서 작성
- `docs/PAPER_IMPLEMENTATIONS.md` 신규 생성
- 7개 논문 각각의 코드 레벨 구현 추적 (파일 경로, 핵심 함수, 데이터 흐름)
- 전체 파이프라인에서 7개 논문이 유기적으로 연결되는 통합 흐름도 포함

## 현재 상태

### 커밋 이력 (이번 세션)
```
9e87ebb docs: 7개 논문 구현 매핑 문서 추가 — 코드 레벨 추적
6277fcc chore: .cli_history 파일 삭제
0faa632 docs: 시스템 아키텍처 SVG 다이어그램 추가
ca0d8e0 docs: 미커밋 세션 인수인계 파일 추가 (#0002, #0003)
2daff02 docs: 구성도 최신화 — Planner Agent + HITL 흐름 + 4-Tier 모델 반영
26ed060 feat: HITL 계획 승인 + CLI 노드 이력 누적 + 문서 최신화
```

### 테스트: 754+ passed (핵심 테스트), CI green
### DashScope 연결: 4티어 모두 정상 동작
### HITL: 계획 표시 → 승인 → 실행 → 파일 저장 E2E 검증 완료

## 다음 단계 (제안)

1. **Planner 페이즈별 순차 실행** — 현재 계획 전문을 한 번에 전달, 향후 페이즈별 순차 실행 구현
2. **Explore Agent 도입** — Claude Code의 Explore Agent 패턴 (read-only 코드베이스 탐색 전문)
3. **Verification Agent 분리** — verify를 별도 에이전트로 분리 (lint, test 기반 도구 실행 검증)
4. **HITL 거부 시 계획 수정** — 현재는 거부 시 중단, 향후 사용자 피드백 반영하여 재계획
5. **스트리밍 UX 고도화** — 코드 생성 중 파일별 진행 상태 표시

## 중요 참고사항

- `astream_events`에서 `GraphInterrupt`는 exception으로 전파되지 않음 → `aget_state()` 기반으로 감지해야 함
- `.env`의 `DASHSCOPE_BASE_URL`은 `/compatible-mode/v1` 경로가 맞음 (`/api/v1`은 404)
- DashScope 워크스페이스: `ap-southeast-1` (싱가포르 리전)
- `fibonacci.py`(test_project에서 생성된 파일)에 구문 오류 있음 — ruff exclude 필요
- SVG 구성도(`docs/architecture_diagram.svg`)는 이미 Planner/HITL/4-Tier 반영 완료 상태
