# 세션 인수인계: 2026-04-06 #0001

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/sds/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

> 이전 세션(#0008, 2026-04-05)에서 이어서 진행. DashScope/Qwen3 환경 설정 → 웹 리서치 기능 → 멀티phase 순차 실행 → 버그 수정까지 대규모 기능 추가.

### 1. DashScope/Qwen3 환경 설정 (.env, .env.example)
- `LLM_PROVIDER=dashscope` 기본 프로바이더로 전환
- `DASHSCOPE_API_KEY`, `DASHSCOPE_BASE_URL` 설정 (국제판: `dashscope-intl.aliyuncs.com/compatible-mode/v1`)
- `.env.example`에 DashScope/OpenRouter 양쪽 가이드 추가
- `core/config.py`: `MODEL_PROVIDER` 폴백으로 `LLM_PROVIDER` 참조하도록 수정
- 워크스페이스 URL(`maas.aliyuncs.com`)은 콘솔용이고, API는 `dashscope-intl.aliyuncs.com` 사용해야 함

### 2. Planner 웹 리서치 기능 (멀티라운드)
- Planner에 `research_external` 노드 추가 — 웹 검색으로 외부 API 문서 조사
- **멀티라운드 리서치**: 1차 검색 → 요약 → 미확인 항목 있으면 후속 쿼리로 2차 검색
- `ANALYZE_SYSTEM_PROMPT`에 `needs_research`, `research_queries` 출력 추가
- `RESEARCH_SYSTEM_PROMPT`, `RESEARCH_SUMMARIZE_PROMPT` 신규 프롬프트
- `PLAN_SYSTEM_PROMPT`에 `{research_context}` 플레이스홀더 + "조사된 정보만 사용" 원칙
- CLI에 `✓ 외부 API 조사 — 6건 조사 (...)` 이력 표시
- 검증 결과: DashScope URL(`dashscope-intl.aliyuncs.com/compatible-mode/v1`), 모델명(`qwen-turbo`) 정확하게 코드에 반영됨

### 3. Planner 페이즈별 순차 실행
- Orchestrator `delegate`에서 멀티phase 분기: phase 2개 이상이면 `_execute_phases_sequentially()` 호출
- `_invoke_planner` → `(task_plan_structured, plan_text)` 튜플 반환으로 변경
- `OrchestratorState`에 `task_plan_structured`, `phase_results` 필드 추가
- `_build_phase_message`: 이전 phase 파일 경로만 안내 (내용은 MCP read_file로)
- 마지막 phase에 통합 체크리스트 자동 추가 (Blueprint 등록, CORS, import 경로 등)
- `PLAN_SYSTEM_PROMPT` 원칙 7-8 추가: phase 수 최소화, import 경로, CORS, 앱 진입점 통합
- CLI에 phase별 진행 표시: `✓ 에이전트 위임 — phase_1: Backend [OK] 4파일 | phase_2: Frontend [OK] 8파일`

### 4. 프론트엔드 파일 저장 수정
- `CodingConfig.allowed_extensions`에 `.html`, `.css`, `.scss`, `.vue`, `.jsx`, `.tsx` 추가
- `EXECUTE_SYSTEM_PROMPT`에 "파일당 1개 코드 블록, 합치지 마세요" + Vue filepath 예시 추가

### 5. 고아 tool_calls 메시지 정합성 버그 수정
- **원인**: budget 초과로 강제 중단 시, AI 메시지의 tool_calls에 대응하는 ToolMessage가 없어 DashScope 400 에러
- **수정**: `core/tool_call_utils.py`에 `sanitize_messages_for_llm()` 추가
- `_execute_code`, `_generate_final`에서 `state["messages"]` 대신 `sanitize_messages_for_llm(state["messages"])` 사용

## 현재 상태

### 커밋 이력 (이번 세션): 아직 미커밋 — 이 세션 파일과 함께 커밋 예정

### 테스트: 755 passed (기존과 동일), CI 영향 없음

### 검증 완료 항목
- DashScope Qwen3 API 연결: qwen-turbo, qwen-plus 정상 동작
- 웹 리서치: 멀티라운드 검색 → DashScope URL/모델명 정확 반영
- 멀티phase 순차 실행: 칸반 보드(15파일, 1019줄) 4/4 phase 완료
- 고아 tool_calls 버그: 수정 후 TurnBudget 초과 시에도 정상 완료

## 다음 단계 (제안)

1. **Phase 4 빈 phase 문제**: TurnBudget 감소수익 감지가 너무 빨리 발동 → 통합 phase에서 코드 생성이 안 됨. budget 파라미터 조정 또는 통합 phase에는 budget 완화 필요
2. **import 경로 일관성**: `from backend.models import db` vs `from models import db` — Planner instructions 품질 개선 필요
3. **requirements.txt 완전성**: flask-cors, pydantic 등 코드에서 사용하는 패키지가 누락되는 경우 있음
4. **Verification Agent 분리**: verify를 별도 에이전트로 분리 (lint, test 기반 도구 실행 검증)
5. **HITL 거부 시 재계획**: 현재 거부 시 중단, 향후 피드백 반영하여 재계획

## 변경된 파일 목록

### 신규
- `agents/planner/prompts.py` — RESEARCH_SYSTEM_PROMPT, RESEARCH_SUMMARIZE_PROMPT 추가

### 수정
- `.env`, `.env.example` — DashScope 기본 프로바이더 전환
- `core/config.py` — MODEL_PROVIDER → LLM_PROVIDER 폴백
- `core/model_tiers.py` — DASHSCOPE_BASE_URL fallback URL
- `core/tool_call_utils.py` — sanitize_messages_for_llm() 추가
- `agents/planner/agent.py` — research_external 노드, 멀티라운드 리서치, 3-way 라우팅
- `agents/planner/config.py` — web_search_mcp_servers, web_search_tools 추가
- `agents/planner/schemas.py` — research_context 필드 추가
- `agents/planner/prompts.py` — ANALYZE/PLAN 프롬프트 대폭 개선
- `agents/orchestrator/agent.py` — _execute_phases_sequentially, _build_phase_message, _invoke_planner 튜플 반환
- `agents/orchestrator/schemas.py` — PhaseResult, task_plan_structured, phase_results 추가
- `agents/coding_assistant/agent.py` — sanitize_messages_for_llm 적용
- `agents/coding_assistant/config.py` — allowed_extensions 확장
- `agents/coding_assistant/prompts.py` — filepath 규칙 강화
- `cli/app.py` — research_external 노드 라벨, phase_results 표시, delegate 요약

## 중요 참고사항

- DashScope 국제판 URL: `https://dashscope-intl.aliyuncs.com/compatible-mode/v1` (워크스페이스 URL 아님)
- 워크스페이스 URL(`ws-*.maas.aliyuncs.com`)은 콘솔 접속용, API 호출 불가
- `sanitize_messages_for_llm`은 DashScope의 엄격한 메시지 검증에 대한 방어 — OpenAI는 관대하지만 DashScope는 고아 tool_calls를 400으로 거부
- 멀티phase 실행 시 각 phase마다 새 CodingAssistantAgent를 생성 (state 격리, MCP 재로드 ~100ms)
