# 세션 인수인계: 2026-04-08 #0001

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — day08 아키텍처 구현
- **저장소**: /home/sds/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

> day08 아키텍처 구현: LiteLLM Gateway + MCP 통합 + Docker 4컨테이너 + make setup 원커맨드 온보딩

### 1. `make setup` 원커맨드 온보딩 구축
- `scripts/setup.sh` 전면 개선 (5단계 → 6단계)
  - Step 4: 대화형 API 키 입력 (메인/백업 프로바이더 순서대로 둘 다 입력)
  - Step 5: Docker Harness 전체 빌드 + 기동
  - Step 6: 서비스 헬스체크 (120초 폴링)
- API 키 입력 UX: `read -s` 대신 한 글자씩 `*` 표시 + 입력 후 글자 수/마스킹 피드백
- `scripts/health_check.sh` 신규 생성 (단독 실행 + `--wait` 폴링 모드)
- `Makefile`에 `up-harness`, `down-harness`, `logs-harness`, `ps-harness`, `health-check` 타겟 추가
- `.env.example` 정규화 (플레이스홀더 제거, 빈값 통일, sed 치환 대상 정리)

### 2. LiteLLM Gateway 패턴 도입
- `langchain-litellm` 패키지 추가 (`pyproject.toml`)
- `core/model_tiers.py` 핵심 변경:
  - `create_chat_model()`: 프로바이더별 if/elif 분기 제거 → LiteLLM 통합
  - 프록시/SDK 듀얼 모드: `LITELLM_PROXY_URL` 유무로 자동 전환
  - `_to_litellm_model()` 레거시 호환 헬퍼 추가
  - `build_default_tiers()`: 모델명 LiteLLM 포맷으로 변경 (dashscope/모델, openrouter/제공자/모델)
  - `_MODEL_COST_DB` 키 LiteLLM 포맷으로 업데이트

### 3. LiteLLM Proxy Docker 서버 분리 (day08 아키텍처 일치)
- `docker/litellm_config.yaml` 신규: 15개 모델 정의 + Langfuse 콜백 + master key
- `docker/docker-compose.harness.yml`: LiteLLM Proxy + PostgreSQL 서비스 추가
- 에이전트 컨테이너에서 API 키 제거 → 프록시 경유 (`LITELLM_PROXY_URL`)
- LiteLLM UI 활성화: `http://localhost:4000/ui` (admin / LITELLM_MASTER_KEY)
- Langfuse 자동 트레이싱: 프록시 레벨 `callbacks: ["langfuse"]`

### 4. MCP 서버 통합
- Tavily, ArXiv, Serper 검색 도구 → `mcp_servers/code_tools/server.py`에 통합
- 단일 MCP 서버 15개 도구 (코드 11 + 검색 4)
- `docker/Dockerfile.mcp` 단순화 (개별 런처 제거)
- Docker Compose: MCP 4개 서비스 → 1개로 축소

### 5. 에이전트 서버 프록시 모드 호환
- `tests/run_coding_assistant_server.py`: `LITELLM_PROXY_URL` 설정 시 API 키 검증 스킵
- `tests/run_orchestrator_server.py`: 동일 수정

## 현재 상태

### Docker Harness (4+1 컨테이너, 모두 healthy)
```
litellm-db       — PostgreSQL 16 (LiteLLM 내부 DB)
litellm-proxy    (:4000)  — LLM Gateway + UI + Langfuse 트레이싱
mcp-server       (:3003)  — 통합 MCP (15개 도구)
agent-coding     (:18084) — CodingAssistant 에이전트
orchestrator     (:18080) — Orchestrator 에이전트
```

### 아키텍처 (day08_architecture.png 일치)
```
Agent → LiteLLM Proxy → DashScope (메인) / OpenRouter (백업)
  │         │
  │         └→ Langfuse (자동 트레이싱)
  └→ MCP Server (코드 + 검색 도구)
```

### 테스트 결과
- 851 passed (유닛 테스트)
- 95 passed (model_tiers + performance)
- Docker 헬스체크 4/4 통과

### 변경된 파일 (주요)
1. `scripts/setup.sh` — 6단계 대화형 온보딩
2. `scripts/health_check.sh` — 신규, 서비스 헬스체크
3. `core/model_tiers.py` — LiteLLM Gateway, 프록시/SDK 듀얼 모드
4. `docker/litellm_config.yaml` — 신규, 15개 모델 + Langfuse
5. `docker/docker-compose.harness.yml` — 4컨테이너 아키텍처 + PostgreSQL
6. `docker/Dockerfile.mcp` — 통합 MCP 이미지
7. `mcp_servers/code_tools/server.py` — 검색 도구 4개 추가 (15개 통합)
8. `Makefile` — harness 타겟 5개 추가
9. `.env.example` — 정규화 + LiteLLM/프록시 설정 추가
10. `pyproject.toml` — langchain-litellm, tavily-python, arxiv, httpx 추가
11. `tests/run_coding_assistant_server.py` — 프록시 모드 호환
12. `tests/run_orchestrator_server.py` — 프록시 모드 호환
13. `tests/test_model_tiers.py` — LiteLLM 포맷 assertion 수정
14. `tests/test_performance.py` — LiteLLM 포맷 키 수정

## 다음 단계 (제안)

1. **PMS 3차 E2E 실행**: Docker Harness 환경에서 PMS 프롬프트 재실행
2. **LiteLLM Fallback 설정**: litellm_config.yaml에 메인→백업 자동 전환 라우팅 규칙 추가
3. **Langfuse 연동 확인**: Langfuse Cloud에서 트레이싱 데이터 수신 확인
4. **vLLM On-Prem 추가**: litellm_config.yaml에 로컬 vLLM 엔드포인트 추가 (향후)
5. **make setup 별도 리포 테스트**: 클린 환경에서 전체 온보딩 플로우 검증

## 중요 참고사항

- **LiteLLM UI**: `http://localhost:4000/ui` → admin / sk-harness-local-dev
- **프록시/SDK 듀얼 모드**: `LITELLM_PROXY_URL` 환경변수 유무로 자동 전환 (Docker=프록시, 로컬개발=SDK)
- **메인/백업 프로바이더**: make setup에서 둘 다 입력받아 .env에 저장, litellm_config.yaml에 양쪽 모두 등록됨
- **MCP 통합 서버**: 포트 3003 하나로 코드+검색 도구 15개 제공
- **에이전트 API 키 불필요**: Docker 환경에서 에이전트는 LiteLLM Proxy만 바라봄
