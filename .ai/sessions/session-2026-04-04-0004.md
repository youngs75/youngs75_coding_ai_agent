# 세션 인수인계: 2026-04-04 #0004

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/youngs75/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

### 1. CI/CD 파이프라인 검증 및 수정
- `ruff check`: 99개 에러 수정 (76개 자동 + 23개 수동)
- `ruff format`: 100개 파일 포맷 적용
- `demo_interactive.py`: `args` 미정의 버그 수정
- CI 테스트 실패 수정: API 키 없을 때 LLM 테스트 skip 처리
- **GitHub Actions CI 전체 통과 확인**: Lint ✓ / Test ✓ / Docker Build ✓

### 2. OpenRouter 오픈소스 모델 전환
- **STRONG**: gpt-5.4 → `qwen/qwen3-coder` ($0.22/M, 코드 생성 특화)
- **DEFAULT**: gpt-5.4 → `deepseek/deepseek-v3.2` ($0.26/$0.38, 추론)
- **FAST**: gpt-4.1-mini → `qwen/qwen3.5-9b` ($0.05/$0.15, 파싱)
- 레거시 `get_model()` → `create_chat_model()` 통합 (OpenRouter 지원)
- 전체 코드베이스 34개 파일 모델 참조 업데이트
- DeepEval OpenRouter 모델 ID 지원 (base_url 자동 전환)

### 3. CLI 개선
- `load_dotenv()` 추가 (`.env` 자동 로드)
- LangSmith tracing 비활성화 (미사용, 401 에러 제거)
- `parse_request`/`verify_result` 노드의 내부 JSON 스트리밍 필터링
- 검증 결과를 사용자 친화적 요약으로 표시 (✓ 통과/✗ 실패 + 제안 건수)

### 4. Docker MCP 서버 수정
- `docker-compose.mcp.yml`: command를 Python 스크립트 직접 실행으로 수정
- `code_tools` MCP 서비스 추가 (파일 I/O, 코드 실행)
- Dockerfile.mcp: code_tools 패키지 구조 복사 추가
- **Docker MCP 전체 정상 기동 확인** (5개 컨테이너)

### 5. Claude Code & Codex 소스코드 분석
- Claude Code (TypeScript/Bun) 전체 아키텍처 분석
- Codex (Rust) 전체 아키텍처 분석
- 11개 핵심 영역 비교 분석 보고서 작성 (`.ai/analysis/claude-code-and-codex-analysis.md`)
- Gemini/Claude 웹 리뷰 수렴 → 프로덕션 관점 로드맵 수립

### 6. Phase 10: 프로덕션 강화 (에이전트 팀 병렬 실행)

#### 6.1 자동 컨텍스트 컴팩션 (`core/context_manager.py`)
- ContextManager: 토큰 추적 + 80% 초과 시 LLM 기반 자동 요약
- max_tokens 복구: stop_reason 감지 → 컴팩션 후 최대 3회 재시도
- 선택적 히스토리 전파: `truncate_for_subagent()` — LastNTurns 패턴
- CodingAssistantAgent 3개 노드 (parse/execute/verify)에 통합
- OrchestratorAgent에 서브에이전트 히스토리 필터링 적용

#### 6.2 도구 병렬 실행기 (`core/parallel_tool_executor.py`)
- ParallelToolExecutor: concurrency_safe 도구 asyncio.gather 병렬 실행
- non-concurrent 도구(bash, write_file) 독점 순차 실행
- 결과 순서 보장 + 부분 실패 핸들링 + 타임아웃 지원

#### 6.3 apply_patch + str_replace MCP 도구
- `apply_patch`: Unified diff 파싱 → 헝크 역순 적용 → 새 파일 생성 지원
- `str_replace`: 정확한 문자열 교체 (0매치: 에러, 2+매치: 모호성 에러)
- workspace 경로 안전 검증

#### 6.4 프로젝트 컨텍스트 주입 (`core/project_context.py`)
- ProjectContextLoader: `.agent/context.md`, `AGENTS.md`, `.ai_agent.md` 자동 발견
- 상위 3단계 디렉토리 검색 + 토큰 예산 관리
- 시스템 프롬프트에 동적 주입 + CLI 초기화 시 로드

#### 6.5 도구 권한 모델 (`core/tool_permissions.py`)
- ToolPermissionManager: ALLOW/ASK/DENY 3단계 권한
- 계층적 설정: 기본 → 프로젝트(.agent/permissions.yaml) → 환경변수
- 민감 파일 패턴 감지 (.env, credentials, .pem 등)
- 거부 기록 로깅

## 현재 상태
- **Phase 1~10 전체 구현 완료**
- 전체 테스트: **571 passed, 1 timeout(deep_research), 43 skipped**
- Phase 10 신규 테스트: 105개
- 총 변경: +3,490줄, 16개 신규/수정 파일
- CI/CD: GitHub Actions 3개 Job 전체 통과

## 다음 단계 후보
1. **훅 시스템** — pre/post tool_call 이벤트 훅 (Claude Code 패턴)
2. **Coordinator Mode 강화** — 로컬 병렬 워커 오케스트레이션
3. **CLI에 Phase 10 기능 통합** — ParallelToolExecutor, ToolPermissionManager를 실제 에이전트 루프에 연결
4. **E2E 통합 테스트** — 실제 LLM + MCP로 전체 파이프라인 검증
5. **Langfuse 대시보드 확인** — 오픈소스 모델 전환 후 트레이스 확인
6. **Prime Jennie 연동 설계** — Self-evolving 코딩 하네스로서의 API 인터페이스

## 중요 참고사항
- **모델**: OpenRouter 오픈소스 (STRONG: qwen/qwen3-coder, DEFAULT: deepseek/deepseek-v3.2, FAST: qwen/qwen3.5-9b)
- **패키지 구조**: import는 `youngs75_a2a`. 패키지 변경 후 `uv pip install -e .` 필수.
- **a2a 디렉토리**: `a2a_local/`로 리네이밍됨 (a2a-sdk 충돌 방지)
- **테스트 실행**: `source .venv/bin/activate && python -m pytest tests/ --ignore=tests/eval/ -v`
- **CLI 실행**: `youngs75-agent` (별도 터미널에서)
- **MCP 서버**: `cd docker && docker compose -f docker-compose.mcp.yml up -d`
- **Makefile**: `make help`로 전체 명령어 확인
- **분석 문서**: `.ai/analysis/claude-code-and-codex-analysis.md` — Claude Code & Codex 비교 분석
- **Claude MAX 200 구독** + $200 무료 크레딧 (2026-04-17까지)
- **프로덕션 목표**: Prime Jennie Self-evolving 코딩 하네스 — 교육 과제 수준이 아닌 프로덕션급
- **.bashrc**: claude 명령어가 tmux 세션 자동 생성/연결하도록 alias 설정됨
