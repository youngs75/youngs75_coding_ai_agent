# 세션 인수인계: 2026-04-09 #0005

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Coding Agent Harness)
- **목표**: Agentic Loop 복원력 강화 + 코드 생성 품질 개선
- **패키지명**: `coding_agent` (실제 디렉토리 구조)

## 이번 세션 작업 내역

### 1. LLM 무한 대기 방지 (요구사항 3-3: 모델 무응답/지연)
- `core/middleware/chain.py`: `_final_handler`에 `asyncio.wait_for(timeout)` 추가
- `core/config.py`: `get_request_timeout(purpose)` 헬퍼 추가
- 4개 에이전트(coding_assistant, orchestrator, planner, verifier): `MWRequest.metadata["request_timeout"]`에 티어별 timeout 전달
- 티어별 timeout: FAST=60s, DEFAULT=120s, STRONG=180s, REASONING=180s
- timeout 초과 시 `asyncio.TimeoutError` → `ResilienceMiddleware`의 `RetryWithBackoff` → `ModelFallbackChain`

### 2. StallDetector FORCE_EXIT 실효성 확보 (요구사항 3-3: 반복 무진전 루프)
- **근본 버그 발견**: StallDetector가 FORCE_EXIT을 발행해도 `_should_retry`/`_should_retry_tests`에서 `exit_reason`을 확인하지 않아 무한 루프 발생
- `_should_retry`: `exit_reason` 체크 추가 → FORCE_EXIT 후 재시도 방지, `APPLY_CODE`로 진행
- `_should_retry_tests`: `exit_reason` 체크 추가 → 테스트 루프에서도 안전 탈출
- `_generate_final` 진입 시 `stall_detector.reset()` → execute↔generate 카운트 분리

### 3. verify 피드백 루프 완성
- **근본 원인 발견 (Langfuse 분석)**: verify 실패 후 재생성 시 STRONG 모델이 189 토큰(이슈 목록만)으로 호출됨 → 이전 코드를 모름 → `read_file`로 읽으려 함 → StallDetector 차단 → 수정 불가 → 같은 이슈 반복
- `_build_context_message`: verify 실패 시 `state["generated_code"]`를 context에 포함 (최대 8000자)
- `_verify_result`: `exit_reason: ""` 리셋 → 재시도 시 잔존 방지
- `_verify_result`: `_prev_generated_code` 저장 → 테스트 실패 시 동일 코드 반복 감지 활성화

### 4. Pydantic 직렬화 경고 해결
- **원인 추적**: `with_structured_output()` 기본 method `json_schema` → OpenAI SDK의 `ParsedChatCompletionMessage.parsed` 필드 → LiteLLM/Langfuse 직렬화 시 경고
- 모든 `with_structured_output()` 호출에 `method="function_calling"` 추가 (4곳)
  - `agent.py`: ParseResultModel, FileManifest, VerifyResultModel
  - `model_tiers.py`: 범용 structured output

### 5. Docker 환경 설정 수정
- Orchestrator에 `CODE_TOOLS_MCP_URL`, `CODE_TOOLS_WORKSPACE`, `workspace_data` volume, `mcp-server` dependency 추가
- Orchestrator가 CodingAssistant를 SubAgent로 spawn할 때 MCP 서버 연결 실패 문제 해결

### 6. 테스트 인프라 개선
- `pyproject.toml`: `--ignore=tests/eval` 추가 (deepeval collection 블로킹 방지)
- `test_docker_e2e.py`: 현재 하니스 구성(mcp:3003, agent:18084, orch:18080)에 맞게 전면 업데이트
- MCP `/mcp` 엔드포인트 406 응답 코드 허용 추가

## 현재 상태

### 검증 결과
- **Docker**: 5개 서비스 모두 healthy (재빌드 완료)
- **pytest**: 371 passed, 5 skipped (Docker E2E 1개 실패 — MCP 연결 타이밍)
- **Langfuse**: 트레이싱 정상 동작 확인

### LLM 프로바이더
- `LLM_PROVIDER=openrouter` (OpenRouter 경유 Qwen3 시리즈)
- REASONING: qwen3-max, STRONG: qwen3-coder-plus, DEFAULT: qwen3-coder-next, FAST: qwen3.5-flash

## 다음 단계 (TODO)

### 1. 칸반보드 E2E 코드 생성 검증 (최우선)
- Orchestrator A2A API(`http://localhost:18080`)로 칸반보드 요청을 직접 호출
- Langfuse tracing API + Docker 로그로 LLM 호출 흐름 실시간 추적
- 코드가 정상 생성될 때까지 반복 이터레이션:
  1. A2A 요청 → 응답 확인
  2. Langfuse에서 각 단계(분류→계획→파싱→생성→검증→테스트) 토큰/latency 분석
  3. 실패 시 원인 파악 → 코드 수정 → Docker 재빌드 → 재시도
- 이전 세션에서 발견된 문제: Orchestrator→CodingAssistant SubAgent spawn 시 MCP URL 누락 (이번 세션에서 docker-compose.yml 수정 완료, 재빌드 완료)

### 2. Langfuse 관측성 확인
- Structured Output 응답이 Langfuse에 정상 기록되는지 확인
- FileManifest 노드의 토큰 사용량 모니터링
- method="function_calling" 전환 후 트레이싱 정상 여부

### 3. 전체 티어 SLM 전환 테스트
- 150B 미만 모델로 전체 티어 매핑 후 품질 비교

## 중요 참고사항

### Langfuse Tracing API 활용법
```bash
# 최근 generation 조회
curl -s -u "pk-lf-fe9f8d31...:sk-lf-885abaf5..." \
  "https://cloud.langfuse.com/api/public/observations?limit=20&type=GENERATION&orderBy=startTime.desc"

# 특정 trace 상세 조회
curl -s -u "..." "https://cloud.langfuse.com/api/public/traces/{trace_id}"
```

### A2A 호출 방법
```bash
curl -s -X POST http://localhost:18080 \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":"test","method":"message/send","params":{"message":{"messageId":"msg-1","role":"user","parts":[{"kind":"text","text":"요청 내용"}]}}}'
```

### Docker 재빌드
```bash
make down && make up  # 전체 재빌드 + 기동
docker logs harness_orchestrator 2>&1 | grep -v "GET /health"  # 로그 확인
```

### SubAgent spawn 방식
- Orchestrator는 CodingAssistant를 A2A가 아닌 **프로세스 내 직접 호출**(SubAgentProcessManager.spawn_and_wait)로 실행
- 따라서 CodingAssistant 컨테이너(harness_agent_coding)에는 요청이 가지 않음
- 코드 생성은 Orchestrator 컨테이너(harness_orchestrator) 내부에서 실행됨
