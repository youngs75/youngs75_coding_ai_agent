# 세션 인수인계: 2026-04-06 #0002

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/sds/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

> 이전 세션(#0001, 2026-04-06)에서 이어서 진행. 5개 이슈 수정 → 범용화(A/B/C) → 실행 기반 검증 → venv 격리 실행까지 대규모 기능 추가.

### 1. 이전 세션 이슈 5건 수정
- **Phase 4 빈 phase 문제**: CodingConfig에 `diminishing_streak_limit`, `min_delta_tokens` 추가. 마지막 phase에 lenient config 적용.
- **import 경로 일관성**: EXECUTE_SYSTEM_PROMPT + PLAN_SYSTEM_PROMPT + _build_phase_message에 import 규칙 강화.
- **requirements.txt 완전성**: EXECUTE/VERIFY 프롬프트에 의존성 관리 섹션 추가.
- **Verification Agent 분리**: `agents/verifier/` 신규 — lint + test + LLM 리뷰 3중 검증 에이전트.
- **HITL 거부 시 재계획**: CLI에서 피드백 수집 → `Command(resume={"approved": False, "feedback": "..."})` → Planner 재호출 (최대 3회).

### 2. 범용화 — 과적합 해소 (A/B/C)
사용자의 "특정 프로그램에 특화되도록 휴리스틱하게 튜닝된 것 아닌지" 우려에 대응.

**A. 하드코딩 → 동적 감지:**
- PLAN_SYSTEM_PROMPT: Flask 고정 예시 제거 → `project_type` 필드 추가 (webapp/cli/library/data_pipeline/api_server/desktop/game/script/other)
- 통합 체크리스트: "Blueprint, CORS" → 5가지 프로젝트 유형별 분기
- Phase budget 완화: 무조건 → 파일 수/instructions 길이로 통합 성격 판단 후 적용
- 언어 자동 감지: `_detect_language()` — 12개 언어 확장자 매핑

**B. 예시 다양화:**
- EXECUTE_SYSTEM_PROMPT: import 규칙 5개 언어, filepath 주석 6가지 형식, 의존성 6개 패키지 관리자
- allowed_extensions: 18개 → 35개 (.go, .rs, .java, .c, .cpp, .rb, .swift, .kt, .cs 등)
- test_generation.yaml: pytest → 6개 프레임워크 (pytest/Jest/testing/cargo test/JUnit/xUnit)
- debug.yaml: Python → 5개 언어별 에러 패턴

**C. Verifier 멀티언어:**
- lint: Python(py_compile), JS(node --check), Go(go vet), Rust(cargo check), Java(javac)
- test: Python(pytest), JS(jest), Go(go test), Rust(cargo test)
- `_LANG_EXTENSIONS` 10개 언어 매핑

### 3. 실행 기반 검증으로 교체
기존: LLM이 코드를 "읽고 판단" → 새: **실제 실행 기반 검증**

**새 그래프 흐름:**
```
GENERATE_FINAL → VERIFY(LLM) → APPLY_CODE → RUN_TESTS → [pass] → END
                                              ↑                    [fail] → GENERATE_FINAL(에러 주입) → ...
```

- `_run_tests` 노드: syntax check → import check → pytest 실행
- `_should_retry_tests` 라우터: 실패 시 에러 메시지 주입 후 GENERATE_FINAL 재호출 (최대 3회)
- `_inject_test_failure`: 테스트 에러를 HumanMessage로 주입하여 LLM이 수정 코드 생성
- EXECUTE_SYSTEM_PROMPT: "테스트 코드 필수 생성" 섹션 추가
- CodingState: `test_output`, `test_passed` 필드 추가

### 4. run_shell MCP 도구 추가
`mcp_servers/code_tools/server.py`에 shell 명령어 실행 도구 추가.
- 화이트리스트: pip, npm, go, cargo, pytest, jest, git 등 28개 명령어
- 블랙리스트: rm -rf /, sudo, | sh 등 위험 패턴 차단
- 파이프라인 검증, 타임아웃 300초, 출력 truncate

### 5. 프로젝트 환경 격리 (venv)
에이전트 자체 venv가 아닌 **workspace에 격리된 venv를 생성**하여 테스트 실행.

- `_detect_runtimes()`: python/node/java/go/rust 런타임 존재 확인
- `_setup_project_env()`: workspace에 `.venv` 생성 (uv venv 우선)
- `_install_dependencies()`: 격리된 venv에 requirements.txt + pytest 설치
- `_run_tests()`: workspace venv의 python으로 pytest 실행 (`-p no:cacheprovider` 플러그인 충돌 방지)

### 6. Planner 규칙 추가 (kanban board 테스트 피드백 반영)
- Phase별 files 분리: "각 phase의 files에는 해당 phase에서 새로 생성할 파일만 포함"
- CORS 자동 포함: 클라이언트-서버 분리 시 flask-cors 필수
- 테스트 파일 필수: 각 phase files에 테스트 파일도 포함 (원칙 4번 신설)
- `__init__.py` 자동 포함: 절대 import 사용 시 file_structure에 필수
- 빈 통합 phase 금지: "통합/테스트/검증만 있는 페이즈 절대 금지"
- 프론트엔드 상태관리: plan에서 확정 후 tech_stack에 포함

### 7. DashScope 400 에러 방어
- `_execute_code`, `_generate_final`에 try/except 추가
- LLM 호출 실패 시 `exit_reason="llm_error"` 설정하여 graceful 전환

## 현재 상태

### 테스트: 818 passed (이전 755 → 818, +63개)

### kanban board 테스트 결과 (3회차)
- Phase 1: 13파일 생성, venv 생성 + 의존성 설치 실행됨 ✓
- Phase 1 테스트: 19개 ERROR (api_bp 순환 import) → 3회 수정 실패 → 최대 반복 도달
- Phase 2: 7파일 생성 (프론트엔드), 테스트 통과 (Python 테스트 없어서 스킵)
- **결론**: venv 격리 + 실제 실행은 작동하지만, LLM의 수정 루프가 순환 import 문제를 해결하지 못함

### Langfuse 검증
- run_tests 노드의 trace에서 test_passed, test_output 필드 확인 완료
- Phase 1: test_passed=False (pytest 19 ERROR)
- Phase 2: test_passed=True ("syntax/import 검증 통과 (테스트 파일 없음)")

## 다음 단계 (제안)

1. **테스트 실패 에러 메시지 분석 강화**: `_inject_test_failure`에서 에러 유형 분석(순환 import, NameError, ModuleNotFound 등) → LLM에게 구체적 수정 지시 제공
2. **Phase files 분배 규칙 미준수**: Planner가 여전히 Phase 1에 13개 파일 할당. 규칙 강화 또는 자동 분할 후처리 로직 검토
3. **프론트엔드 거짓 통과**: Vue/JS 파일에 대해 node --check / tsc --noEmit 문법 검증 추가
4. **순환 import 패턴 방지**: EXECUTE_SYSTEM_PROMPT에 Flask Blueprint 패턴 가이드 추가
5. **HITL 환경 승인**: venv/의존성 설치 전 interrupt로 사용자 확인

## 변경된 파일 목록

### 신규
- `agents/verifier/__init__.py` — VerificationAgent 패키지
- `agents/verifier/agent.py` — lint + test + LLM 리뷰 3중 검증 + 멀티언어
- `agents/verifier/config.py` — VerifierConfig
- `agents/verifier/prompts.py` — 검증 프롬프트
- `agents/verifier/schemas.py` — CheckResult, VerificationResult, VerificationState
- `tests/test_run_tests.py` — 실행 기반 검증 26개 테스트
- `tests/test_verifier.py` — VerificationAgent 테스트

### 수정
- `agents/planner/prompts.py` — PLAN_SYSTEM_PROMPT 전면 범용화 (project_type, files 분배, 테스트 필수, __init__.py, CORS, 상태관리, 빈 phase 금지)
- `agents/coding_assistant/agent.py` — _run_tests(venv 격리), _setup_project_env, _detect_runtimes, _install_dependencies, _should_retry_tests, _inject_test_failure, DashScope 400 방어
- `agents/coding_assistant/config.py` — diminishing_streak_limit, min_delta_tokens, allowed_extensions 35개
- `agents/coding_assistant/prompts.py` — EXECUTE(테스트 필수, import 5언어, filepath 6형식, 의존성 다언어) + VERIFY(의존성 완전성)
- `agents/coding_assistant/schemas.py` — test_output, test_passed 필드
- `agents/orchestrator/agent.py` — 통합 체크리스트 동적화, _detect_language, budget 조건부, Phase 파일 보호, HITL 재계획
- `agents/orchestrator/schemas.py` — verification_result 필드
- `mcp_servers/code_tools/server.py` — run_shell MCP 도구 (화이트리스트 28개 + 보안)
- `cli/app.py` — run_tests 노드 라벨/요약, HITL 피드백 수집
- `cli/renderer.py` — ask_rejection_feedback
- `core/turn_budget.py` — (변경 없음, config에서 파라미터 주입)
- `data/skills/test_generation.yaml` — v2.0.0 멀티언어
- `data/skills/debug.yaml` — v2.0.0 멀티언어
- `tests/test_coding_assistant.py` — run_tests 노드 그래프 구조 검증
- `tests/test_model_tiers.py` — budget config 테스트
- `tests/test_turn_budget.py` — lenient budget 테스트

## 중요 참고사항

- kanban board 테스트에서 `api_bp` 순환 import는 Qwen 모델의 Flask Blueprint 코드 생성 품질 문제. 에이전트 하네스 수정으로 해결할 수 있는 범위(에러 메시지 강화)와 LLM 자체 능력의 한계가 교차하는 지점.
- pytest-asyncio 버전 충돌이 발생하여 pytest를 9.0.2로 업그레이드함.
- `_run_tests`가 workspace에 `.venv`를 생성하므로, 반복 테스트 시 이전 venv가 재사용됨. 깨끗한 테스트가 필요하면 `rm -rf kanban_board/.venv` 후 재실행.
