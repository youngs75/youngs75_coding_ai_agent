# 세션 인수인계: 2026-04-07 #0005

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/sds/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

> skill_registry 전달 재점검 + 에러 분류/반복감지 강화 → 칸반보드 벤치마크 3/3 Phase, 305s, 재시도 0회 달성.

### 1. Orchestrator skill_registry 전달 수정 (핵심)
- `core/skills/registry.py`: `auto_activate_for_task()`에서 `task_type.strip().lower()` 정규화
  - LLM이 "Scaffold", "GENERATE", "fix " 등을 반환해도 매칭되도록 개선
- `agents/orchestrator/agent.py`: multi-phase 경로에서 프레임워크 스킬 사전 활성화
  - `task_plan.framework` 또는 `architecture` 텍스트에서 프레임워크 자동 감지
  - 감지된 스킬을 L2 활성화 후 각 phase 초기 상태에 `skill_context`로 주입
  - 각 phase의 `_parse_request`가 scaffold를 감지하지 못해도 프레임워크 가이드 적용

### 2. HTTP 상태 코드 에러 패턴 추가
- `agents/coding_assistant/agent.py`: `_ERROR_PATTERNS`에 4개 패턴 추가
  - `HTTPMethodNotAllowed` (405): "GET 핸들러가 등록되지 않았습니다" 구체적 수정 지시
  - `HTTPNotFound` (404): Blueprint 등록, url_prefix 확인 안내
  - `HTTPServerError` (500): DB 초기화, 필수 필드 확인 안내
  - `HTTPStatusError` (범용 4xx/5xx): 라우트 등록→메서드→URL→핸들러 순서 점검

### 3. 반복 감지 로직 개선
- `agents/coding_assistant/agent.py`: `_inject_test_failure()`의 반복 감지 2 개선
  - 기존: 정확한 텍스트 매칭 → `assert 405 == 404`와 `assert 404 == 405`를 다른 에러로 판정
  - 개선: 숫자를 N으로 치환하여 정규화 비교 + `_similarity_ratio()` 유사도 85% 이상이면 동일 에러
  - 감지 임계값: iteration ≥ 3 → iteration ≥ 2로 조기 감지

### 4. 불필요한 파일 정리
- `fibonacci.py`, `test_fibonacci.py` (루트): Agent 산출물 잔여 파일 삭제
- `tests/test_fibonacci.py`: 초기 커밋에 잘못 포함된 Agent 산출물 git rm
- `.gitignore`: Agent 산출물 잔여 파일 패턴 추가

### 5. 테스트 추가
- `tests/test_skills.py`: `test_auto_activate_case_insensitive` — 대소문자/공백 normalize 검증

## 현재 상태

### 벤치마크 결과 (칸반 보드 앱)
| 지표 | 5차 (Qwen 2.5, 초기) | 11차 (하네스 개선) | 최신 (이번 세션) |
|------|---------------------|-------------------|-----------------|
| Phase 통과 | 3/4 | 4/4 | **3/3 (100%)** |
| 소요시간 | 798.9s | 399.4s | **305.7s** |
| 재시도 횟수 | 10+ | 2 | **0** |
| 파일 수 | 34 (중복) | 12 (최소) | **20 (완전)** |
| 코드 품질 | 동작 불가 | import 정합 | **백엔드 완벽, FE 구조 양호** |

### Langfuse 트레이싱 결과
- Phase별 소요시간 균등: 89s / 83s / 85s
- LLM 호출 9회 (Phase당 parse→generate→verify 1사이클)
- generate_final이 전체 LLM 시간의 68% (코드 생성이 주 병목 — 정상)
- flask_vue 스킬 활성화 확인 (CLI 출력: "⚙ 스킬 활성화: flask_vue")

### 모델 사용 현황
- CLI 출력: qwen3-coder-next(STRONG), qwen3.5-flash(FAST), qwen3.5-plus(DEFAULT), qwen3-max(REASONING)
- Langfuse: qwen-plus로 기록됨 — DashScope가 qwen3 모델명을 내부적으로 qwen-plus로 매핑하는 것으로 추정
- DashScope API 직접 테스트: qwen3-coder-next, qwen3.5-plus, qwen3.5-flash 모두 정상 응답 확인

### 테스트: 820 passed (외부 서비스 미실행 관련만 실패)

### 변경된 파일 (5개)
- `core/skills/registry.py` — task_type normalize
- `agents/orchestrator/agent.py` — framework 스킬 사전 활성화 + 초기 상태 주입
- `agents/coding_assistant/agent.py` — HTTP 에러 패턴 4개 + 반복감지 정규화 + _similarity_ratio()
- `tests/test_skills.py` — 대소문자 normalize 테스트
- `.gitignore` — Agent 산출물 잔여 파일 패턴

## 다음 단계 (제안)

1. **프론트엔드 함수명 불일치 개선**: Board.vue에서 createColumn→createNewColumn, createCard→createNewCard 등 composable 함수명과 컴포넌트 호출명이 불일치 — Phase간 네이밍 일관성 보장 로직 필요
2. **Langfuse 모델명 매핑 확인**: DashScope가 qwen3-* 를 qwen-plus로 리포트하는지, 아니면 실제로 다른 모델이 사용되는지 추가 검증
3. **Langfuse 토큰 추적**: DashScope(OpenAI-compatible) 연동 시 토큰 카운트가 0으로 기록됨 — usage 정보 전달 설정 필요
4. **Procedural Memory 저장 확인**: 이번 실행에서 재시도 0회라 _save_fix_pattern 미발동 — 의도적 실패 테스트로 저장 로직 검증 필요
5. **프론트엔드 통합 테스트**: Vue 컴포넌트가 실제 브라우저에서 동작하는지 E2E 테스트 추가

## 중요 참고사항

- **flask_vue 스킬이 핵심 기여**: 재시도 0회 달성의 주 요인. 스킬 주입으로 LLM이 첫 시도에 올바른 코드 생성
- **HTTP 에러 패턴/반복감지 개선은 안전망**: 이번 실행에서는 미발동, 향후 재시도 발생 시 효과 기대
- **Qwen3 모델 전환 완료**: DashScope API에서 정상 응답 확인, CLI에서도 올바른 모델명 표시
- **Phase 분할 3개가 최적**: 백엔드(1) → FE 기본(2) → FE UI(3) 구성이 균등한 소요시간 달성
