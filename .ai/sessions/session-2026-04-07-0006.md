# 세션 인수인계: 2026-04-07 #0006

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/youngs75/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

> 최종 과제 요구사항(Day 9-10 Mini PJT) 대비 Gap 분석 → 3개 핵심 축 병렬 구현 → 통합 → Docker Harness 가동 검증

### 1. 과제 요구사항 분석 및 Gap 도출
- Notion 페이지(Day 9-10 Mini PJT) 요구사항 3개 핵심 축 확인:
  1. 장기 메모리 3계층 (user/profile, project/context, domain/knowledge)
  2. 동적 SubAgent 8단계 수명주기 관리
  3. Agentic Loop 복원력 (7가지 장애 유형 중 4개 이상)
- DeepAgents(`/projects/deepagents`) clone + Claude Code 복제본(`/projects/claude-code-haha`) 참고 분석
- 기존 구현 대비 Gap ~30% 도출

### 2. 메모리 3계층 분리 + 영속화 + 정정 API (Task #1)
- `core/memory/schemas.py`: MemoryType에 `USER_PROFILE`, `DOMAIN_KNOWLEDGE` 추가, MemoryItem에 `updated_at`, `source` 필드 추가
- `core/memory/store.py`: `update()` 정정 메서드, `accumulate_user_profile()`, `accumulate_domain_knowledge()` 추가, `_PERSISTENT_TYPES` 상수로 전 계층 JSONL 영속화, `_rewrite_file()` 전체 재작성 메서드
- `core/memory/state.py`: MemoryAwareState에 `user_profile_context`, `domain_knowledge_context` 필드 추가
- 기존 46개 테스트 전부 통과 (하위호환 유지)

### 3. SubAgent 8단계 상태 머신 + 메타데이터 확장 (Task #2)
- `core/subagents/schemas.py`: SubAgentStatus 8단계 확장 (CREATED→ASSIGNED→RUNNING→BLOCKED→COMPLETED→FAILED→CANCELLED→DESTROYED), `VALID_TRANSITIONS` 전이 규칙, `SubAgentInstance` 런타임 인스턴스 클래스, `SubAgentEvent` 상태 전이 이벤트 클래스
- `core/subagents/registry.py`: `create_instance()`, `transition_state()`, `destroy_instance()`, `cleanup_completed()`, `event_log`, `active_instances` 추가
- 기존 12개 테스트 통과 + AVAILABLE/BUSY/DISABLED 하위호환 alias 유지

### 4. Agentic Loop 복원력 강화 (Task #3)
- `core/resilience.py` 신규: `FailureType`(7가지), `FailurePolicy`, `FailureMatrix`(장애 매트릭스 코드화), `RetryWithBackoff`(지수 백오프 재시도), `ModelFallbackChain`(모델 체인 fallback)
- `core/model_tiers.py`: `build_fallback_chain()` helper 함수 추가

### 5. 에이전트 통합 와이어링 (Task #4)
- `agents/coding_assistant/schemas.py`: CodingState에 `user_profile_context`, `domain_knowledge_context` 필드 추가
- `agents/coding_assistant/agent.py`: `_retrieve_memory()`에 USER_PROFILE/DOMAIN_KNOWLEDGE 검색 추가, `_build_execute_system_prompt()`에 새 메모리 컨텍스트 주입
- `agents/orchestrator/coordinator.py`: `_execute_wave()`에 SubAgentInstance 수명주기 통합 (CREATED→ASSIGNED→RUNNING→COMPLETED/FAILED→DESTROYED)

### 6. 테스트 + 증빙 문서 (Task #5)
- `tests/test_resilience.py` 신규 (16개 테스트): FailureMatrix, RetryWithBackoff, ModelFallbackChain
- `tests/test_memory_extended.py` 신규 (9개 테스트): UserProfile, DomainKnowledge, MemoryCorrection
- `tests/test_subagent_lifecycle.py` 신규 (11개 테스트): 전체 수명주기, 실패 재시도, blocked 재개, cleanup
- `REPORT.md` 갱신: 요구사항 ↔ 코드 위치 매핑 섹션 추가

### 7. Docker Harness 가동 검증
- `.env`: 4-Tier 모델을 Qwen3 세대로 갱신 (qwen3-max, qwen3-coder-next, qwen3.5-plus, qwen3.5-flash)
- `docker-compose.harness.yml`: AGENT_MODEL 하드코딩 제거 → .env 4-tier 체계 사용
- `tests/run_coding_assistant_server.py`: `await CodingAssistantAgent.create()` 비동기 팩토리 사용으로 수정
- `tests/run_orchestrator_server.py`: DashScope API 키 지원 추가
- **Docker 6컨테이너 전체 healthy + Qwen3-coder-next로 피보나치 코드 생성 성공 확인**

## 현재 상태

### 테스트 결과
- **876 passed** (기존 820 + 신규 56), 외부 서비스 의존 1개만 실패 (deep_research timeout)

### 요구사항 충족도 (자기 점검 8문항)
1. ✅ user/profile, project/context, domain/knowledge 3계층 분리
2. ✅ 도메인 지식 입력 시 이후 작업에서 재사용 (accumulate + search + prompt injection)
3. ✅ SubAgent 런타임 생성 + 8단계 상태 전이 + 종료 기록
4. ✅ SubAgent blocked/failed 처리 규칙 (VALID_TRANSITIONS + cleanup)
5. ✅ retry(RetryWithBackoff), fallback(ModelFallbackChain), safe stop(AbortController)
6. ✅ StallDetector + TurnBudget + AbortController로 안전 중단 기준
7. ✅ DeepAgents 기준 기능 매핑 (REPORT.md에 명시)
8. ✅ 오픈소스 모델 Qwen3 사용 + DashScope/OpenRouter 지원

### Docker 구성
- 6컨테이너: MCP(code_tools, tavily, arxiv, serper) + CodingAssistant + Orchestrator
- Qwen3 모델 사용, .env 기반 설정
- `cd docker && docker compose -f docker-compose.harness.yml up -d`로 풀 가동

### 변경된 파일 (17개)
- `core/memory/schemas.py` — MemoryType 확장 + MemoryItem 필드 추가
- `core/memory/store.py` — update, accumulate_user_profile, accumulate_domain_knowledge, 전 계층 영속화
- `core/memory/state.py` — user_profile_context, domain_knowledge_context
- `core/subagents/schemas.py` — 8단계 상태, SubAgentInstance, SubAgentEvent, VALID_TRANSITIONS
- `core/subagents/registry.py` — 인스턴스 수명주기 관리 메서드
- `core/subagents/__init__.py` — 새 클래스 export
- `core/resilience.py` — 신규 (FailureMatrix, RetryWithBackoff, ModelFallbackChain)
- `core/model_tiers.py` — build_fallback_chain()
- `agents/coding_assistant/schemas.py` — CodingState 필드 추가
- `agents/coding_assistant/agent.py` — 메모리 검색/주입 확장
- `agents/orchestrator/coordinator.py` — SubAgentInstance 수명주기 통합
- `tests/test_resilience.py` — 신규 (16 tests)
- `tests/test_memory_extended.py` — 신규 (9 tests)
- `tests/test_subagent_lifecycle.py` — 신규 (11 tests)
- `tests/test_cli.py` — semantic_context assertion 완화
- `REPORT.md` — 요구사항 매핑 섹션
- `.env` — Qwen3 모델 갱신
- `docker/docker-compose.harness.yml` — AGENT_MODEL 하드코딩 제거
- `tests/run_coding_assistant_server.py` — 비동기 create + 4-tier 지원
- `tests/run_orchestrator_server.py` — DashScope 키 지원

## 다음 단계 (제안)

1. **E2E 데모 시나리오 녹화**: Docker Harness로 칸반보드 앱 같은 실제 프로젝트 생성 데모
2. **프론트엔드 함수명 불일치 개선**: Phase간 네이밍 일관성 보장 로직
3. **Langfuse 토큰 추적**: DashScope 연동 시 usage 정보 전달 설정
4. **발표 자료 준비**: 요구사항 매핑 + 아키텍처 다이어그램 + 벤치마크 결과

## 중요 참고사항

- **DeepAgents 레퍼런스**: `/projects/deepagents/` — MemoryMiddleware, SubAgentMiddleware 패턴 참고
- **Claude Code 레퍼런스**: `/projects/claude-code-haha/` — memdir/, tasks/ 패턴 참고
- **과제 핵심**: 단순 CRUD 아닌 "왜 그 구조가 필요한지 설명 가능한 상태"
- **모델 정책**: 오픈소스 모델(Qwen3) 가산점, 프론티어 모델(Claude/GPT) 사용 제한
