# 세션 인수인계: 2026-04-10 #0004

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Coding Agent Harness)
- **목표**: 스킬 시스템 연결 + 런타임 버전 안정화 + Planner 간결화 + E2E 품질 향상
- **패키지명**: `coding_agent`

## 이번 세션 작업 내역

### 1. 스킬 시스템 연결 (Critical Fix — 3단계)

#### 1차: _retrieve_memory에서 skill_context 주입 코드 추가
- **파일**: `coding_agent/agents/coding_assistant/agent.py`
- `self._skill_registry.auto_activate_for_task()` → `get_active_skill_bodies()` → `result["skill_context"]`로 주입
- memory_store가 None이면 early return하여 스킬 로직에 도달 못하던 문제 해결 (early return 제거, Memory 블록만 조건부 처리)

#### 2차: logger 변수 미선언 크래시 수정
- `logger = logging.getLogger(__name__)` 추가
- 이전 E2E에서 `name 'logger' is not defined` 에러로 Phase 전체 실패했던 원인

#### 3차: task_type 폴백 문제 수정
- **근본 원인**: `parse_result`가 비어 있어서 `task_type = "generate"` (기본값) → 태그 `["quality"]` → 프레임워크 스킬(`["scaffold", "framework"]`)과 매칭 안 됨
- **수정**: `planned_files`가 있으면 `task_type`을 `"scaffold"`로 변경
- **검증**: `scaffold` + `fastapi_react` → 1개 본문 주입 확인

### 2. Docker 런타임 버전 안정화
| 런타임 | 변경 전 | 변경 후 |
|--------|---------|---------|
| Python | 3.13 | **3.12** |
| Node.js | 22 | **20 LTS** |
| Java | 21 | **21** (유지) |

- **파일**: `docker/Dockerfile.mcp`, `coding_agent/agents/coding_assistant/prompts.py`
- Python 3.13의 SQLAlchemy/Pydantic deprecation 경고 해소 목적

### 3. 프론트엔드 필수 파일 기계적 검증
- **파일**: `coding_agent/agents/coding_assistant/agent.py` (`_run_tests`)
- package.json이 있는 디렉토리에서:
  - TS 파일 존재 시 `tsconfig.json` 누락 검증
  - `react-scripts` 사용 시 `public/index.html` + `src/index.tsx` 누락 검증
- 누락 시 `test_passed = False` 반환하여 LLM이 재생성하도록 유도

### 4. 프롬프트 개선
#### CodingAssistant (prompts.py)
- 프론트엔드 필수 파일 가이드 추가 (tsconfig, index.html, 엔트리포인트)
- 프론트-백엔드 일관성 가이드 (필드명/타입 일치, API 명세 준수)
- npm 패키지 존재 검증 안내
- 런타임 버전 업데이트 (Python 3.12, Node.js 20 LTS, Java 21 LTS)

#### Planner (prompts.py)
- **코드 절대 금지** — "코드 블록을 포함하지 마세요" 명시
- **instructions Phase당 500자 이내** — 사양만 자연어로
- **전체 계획 5000자 이내**
- GLM-5에서 91K 계획서 생성 문제 방지 목적

### 5. GLM-5 + MiniMax M2.5 모델 전환 시도 및 롤백
- **시도**: REASONING=GLM-5, STRONG/DEFAULT=MiniMax M2.5
- **결과**: GLM-5가 91K 계획서 생성 (코드 포함), MiniMax M2.5가 도구 호출 미수행
- **원인**: Qwen3 도구 호출 포맷에 튜닝된 프롬프트/MCP 구성이 다른 모델과 호환 안 됨
- **조치**: Qwen3로 즉시 롤백, .env에서 오버라이드 제거
- **litellm_config.yaml**: MiniMax M2.5, GLM-5 모델 정의는 추가된 상태로 유지 (향후 참고)
- **model_tiers.py**: MiniMax M2.5, GLM-5 비용/컨텍스트 정보 추가됨

### 6. SLM 요구사항 확인
- 4개 티어 중 **1개만 SLM이면 10점 만점** → FAST 티어(qwen3.5-flash)가 이미 SLM → **만점 조건 충족**
- 나머지 3개 티어는 품질 최적화에 집중 가능

### 7. E2E 분석 (v3~v6, 총 4회)

| 버전 | 소요 | LLM 호출 | Phase | 파일 | 백엔드 pytest | 프론트 npm | 스킬 주입 |
|------|------|----------|-------|------|-------------|-----------|----------|
| v3 | 259s | 35회 | 2 | 22 | 6/6 통과 | install 실패 (fake 패키지) | 미주입 |
| v4 | 278s | 40회 | 2 | 24 | 7/7 통과 | install 성공, 빌드 실패 | 미주입 |
| v5 | 964s | 76회 | 3 | 27 | 부분 통과 | 전체 실패 (JSX 루프) | 미주입 |
| v6 | 257s | 27회 | 2 | 19 | 미실행 | 미실행 | 미주입 |

## 현재 상태

### 테스트 결과
- 전체 (eval/docker/e2e 제외): **39 passed, 1 skipped**

### 코드 변경 요약 (7파일, +214줄/-65줄)
| 파일 | 변경 내용 |
|------|----------|
| `coding_assistant/agent.py` | 스킬 주입 3단계 수정 (skill_context, logger, scaffold task_type), 프론트엔드 필수 파일 검증 |
| `coding_assistant/prompts.py` | 런타임 버전, 프론트엔드/일관성 가이드, npm 검증 안내 |
| `planner/prompts.py` | 코드 금지, 500자/5000자 제한 |
| `core/model_tiers.py` | MiniMax M2.5, GLM-5 비용/컨텍스트 정보 추가 |
| `docker/Dockerfile.mcp` | Python 3.12, Node.js 20 LTS |
| `docker/litellm_config.yaml` | MiniMax M2.5, GLM-5 모델 정의 추가 |
| `.ai/memory/semantic.jsonl` | 메모리 데이터 |

## 다음 단계

### 1. 스킬 주입 E2E 검증 (최우선)
- Docker 리빌드 → E2E 실행
- Langfuse 트레이스에서 "활성 스킬" + "FastAPI + React 풀스택 프로젝트의 아키텍처 가이드" 문구 존재 확인
- 스킬 주입으로 인한 코드 품질 변화 측정 (tsconfig 생성 여부, 필드명 일관성 등)

### 2. run_shell 미실행 문제 조사
- v6에서 run_shell이 0회 — LLM이 프롬프트의 "반드시 테스트 실행" 지시를 무시
- Harness 레벨에서 Phase 완료 후 강제 npm install/test 실행 검토

### 3. Planner 기술 스택 일관성
- 사용자가 기술 스택을 지정하지 않으면 Planner가 매번 다르게 선택 (FastAPI vs Express)
- 프롬프트에 기본 스택 가이드 추가 또는 사용자 프롬프트에 명시 권장

### 4. 과제 제출 준비
- SLM 1개 티어 = 만점 조건 이미 충족 (FAST=qwen3.5-flash)
- Embedding 의미 검색, 메모리 TTL 미구현 (점수 영향 낮음)

## 중요 참고사항

### Langfuse 분석 표준 포맷 (사용자 확인)
1. 실행 통계 요약
2. Trace별 LLM 호출 상세 테이블
3. 도구 호출 통계
4. 실제 구동 검증
5. Critical 버그 목록 (P0/P1/P2)
6. 이전 버전 대비 변화
7. 핵심 인사이트

### 모델 전환 교훈
- Qwen3 외 모델(GLM-5, MiniMax M2.5) 전환 시 도구 호출 포맷 호환성 문제 발생
- 프롬프트 + MCP 구성이 Qwen3에 최적화되어 있어 모델 전환은 대규모 재튜닝 필요
- SLM 요구사항은 FAST 티어로 이미 충족되므로 무리한 모델 전환 불필요
