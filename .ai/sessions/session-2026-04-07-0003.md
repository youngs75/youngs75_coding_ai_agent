# 세션 인수인계: 2026-04-07 #0003

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/sds/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

> 프레임워크 템플릿 MCP + Skills 통합 구현.
> TeamCreate로 에이전트 팀(mcp-builder, skill-writer)을 구성하여 병렬 작업 수행.
> Langfuse 트레이스 분석으로 스킬 미활성화 근본 원인을 특정하고 수정.

### 1. Project Creator MCP 도구 구현 → code_tools 서버에 통합
처음에는 별도 MCP 서버(`mcp_servers/project_creator/`)로 구현 후, 코딩 도구 성격이 같으므로 `mcp_servers/code_tools/server.py`에 병합.

- `list_templates()` — 4개 프레임워크 템플릿 목록 반환
- `get_template_info(template_name)` — 상세 정보 (구조, 의존성, 파일 수)
- `create_project(template_name, project_name, options)` — 실제 보일러플레이트 파일 생성
- `_safe_path()` 보안 패턴 적용 (workspace 밖 접근 차단)
- 기존 디렉토리 덮어쓰기 방지

지원 템플릿 4개:
1. `flask-vue` — Flask App Factory + Vue 3 + Vite
2. `react-express` — React + Express.js
3. `fastapi-react` — FastAPI + React + Vite
4. `django-htmx` — Django + HTMX (SSR)

### 2. 프레임워크별 Skills YAML 작성 (4개)
`data/skills/`에 프레임워크별 스킬 YAML 파일 추가:

- `flask_vue.yaml` — App Factory, Blueprint, Vue 3 Composition API, Vite 프록시, CORS 패턴
- `react_express.yaml` — React 함수형 컴포넌트, Express 라우터/미들웨어 패턴
- `fastapi_react.yaml` — FastAPI 라우터/의존성 주입, Pydantic 모델, async 패턴
- `django_htmx.yaml` — Django MTV, CBV, HTMX 속성, 부분 템플릿 패턴

각 스킬 body에 프로젝트 구조, 핵심 패턴 5개, 의존성, 주의사항, 코드 스타일 섹션 포함.

### 3. 에이전트 통합
- `core/skills/registry.py`: `TASK_TYPE_TAGS`에 `"scaffold"`, `"create"` → `["scaffold", "framework"]` 매핑 추가
- `agents/coding_assistant/config.py`: project_creator 별도 엔드포인트 제거 (code_tools 하나로 통합)
- `tests/test_project_creator.py`: 23개 테스트 작성 (MCP 도구 12 + 스킬 로드 7 + 태그 매핑 4)

### 4. 스킬 시스템 Orchestrator 단절 수정 (핵심 버그)
**문제**: Orchestrator → CodingAssistant 호출 시 `skill_registry`를 전달하지 않아, 스킬이 전혀 활성화되지 않음.
- CLI 경로는 정상 (cli/app.py에서 skill_registry 초기화 + 전달)
- Orchestrator 경로만 단절

**Langfuse 트레이스로 확인한 증거**:
- `auto_activate_for_task()` 호출 없음
- 시스템 프롬프트에 스킬 body 주입 없음
- 사용된 도구는 `list_directory`만

**수정 내용**:
1. `agents/orchestrator/agent.py`: `_invoke_local_agent()`에서 SkillRegistry 초기화 + discover() + CodingAssistant에 전달
2. `agents/coding_assistant/prompts.py`: `PARSE_SYSTEM_PROMPT`의 task_type에 `"scaffold"` 추가 ("새 프로젝트/앱 생성" 설명)

### 5. Claude Code auto 모드 설정
`~/.bashrc`에 `alias claude='claude --dangerously-skip-permissions'` 추가.

### 6. MCP 서버 통합 (4→3 서비스)
Docker Compose에서 코딩 관련 MCP 서비스를 1개로 통합:
- **Before**: code-tools(3003) + project_creator(3004) = 2개
- **After**: code-tools(3003) 1개 (11개 도구)
- 리서치 MCP 서버 3개(tavily, arxiv, serper)는 별도 유지 (성격이 다름)

## 현재 상태

### 테스트: 47 passed (test_project_creator 23 + test_skills 24)
기존 테스트 영향 없음.

### 변경된 파일 (8개)
- `mcp_servers/code_tools/server.py` — 프로젝트 템플릿 도구 3개 + TEMPLATES dict 추가
- `data/skills/flask_vue.yaml` — 신규
- `data/skills/react_express.yaml` — 신규
- `data/skills/fastapi_react.yaml` — 신규
- `data/skills/django_htmx.yaml` — 신규
- `core/skills/registry.py` — scaffold/create 태그 매핑 추가
- `agents/orchestrator/agent.py` — skill_registry 초기화 + 전달 (핵심 수정)
- `agents/coding_assistant/prompts.py` — task_type에 scaffold 추가
- `tests/test_project_creator.py` — 신규 (23개 테스트)

### 삭제된 파일
- `mcp_servers/project_creator/` — code_tools에 병합 후 삭제

## 다음 단계 (제안)

1. **kanban board 5회차 테스트**: 이번 수정사항(스킬 활성화 + scaffold task_type) 반영 후 재시도
   - flask_vue 스킬이 시스템 프롬프트에 주입되는지 Langfuse로 확인
   - create_project MCP 도구가 호출되는지 확인
2. **phase 실패 시 결과 표시 개선**: max iterations 도달해도 [OK]로 표시되는 문제 수정
3. **Procedural Memory 연동**: 성공 패턴 자동 저장

## 중요 참고사항

- **스킬 시스템 단절 수정 완료**: Orchestrator → CodingAssistant 경로에서 skill_registry가 이제 정상 전달됨. CLI 경로와 동일하게 작동.
- **scaffold task_type 추가**: "만들어줘", "앱을 생성해줘" 같은 요청이 scaffold로 분류되어 프레임워크 스킬이 자동 활성화됨.
- **MCP 서버 통합 원칙**: 코딩 도구는 code_tools 1개로, 리서치 도구는 별도 서버로 유지.
- **에이전트 팀**: TeamCreate는 병렬 작업이 필요할 때만 사용. 단일 작업이면 Agent 도구로 충분.
