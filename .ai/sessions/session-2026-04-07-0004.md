# 세션 인수인계: 2026-04-07 #0004

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/sds/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

> 칸반 보드 벤치마크 기반 Agent Harness 대폭 개선.
> 5차(Qwen 2.5) → 11차(하네스 개선) 실행으로 4/4 Phase 통과, 소요시간 50% 단축.

### 1. 스킬 시스템 개선
- `_execute_phases_sequentially()`에 SkillRegistry 초기화 + 전달 (멀티 phase 경로)
- PARSE_SYSTEM_PROMPT에 `framework` 필드 추가 → `auto_activate_for_task()`에 framework_hint 전달
- **4개 프레임워크 스킬 v2.0 보강** (awesome-cursorrules 커뮤니티 베스트 프랙티스 병합)
  - flask_vue, fastapi_react, django_htmx, react_express
  - 필수 파일 체크리스트, 완전한 코드 예시, Common Pitfalls, 테스트 fixture 패턴 추가

### 2. scaffold 경로 최적화
- scaffold task → FAST 스킵, STRONG 직행 (저품질 FAST 코드 → 재시도 낭비 제거)
- `_should_use_tools` 라우팅에서 scaffold → GENERATE_FINAL 직행

### 3. Phase 분할 전략 개선 (핵심)
- Planner 프롬프트: "파일 수 기준" → **"테스트 가능한 최소 단위" 기준**으로 변경
- "서로 import하는 파일은 같은 Phase에 배치" 규칙 추가
- MAX_FILES_PER_PHASE: 5 → 8로 상향
- 풀스택 권장 분할: 백엔드 전체(1 Phase) → 프론트엔드(2 Phase)

### 4. 이전 Phase 파일 내용 직접 주입
- `_inject_prev_phase_files()` 함수 추가 (orchestrator/agent.py)
- Phase 2+에서 이전 phase의 핵심 소스 파일(models, routes, config 등) 내용을 메시지에 직접 포함
- 모델 필드명 불일치(name vs title) 문제 해결

### 5. 테스트 출력 개선
- `_smart_truncate_test_output()`: FAILURES/ERRORS 섹션 우선 추출 (3000자)
- CLI 에러 표시: pytest `E   ` 라인 우선 표시 (pytest 헤더 대신 실제 에러)

### 6. 에러 분류 강화
- `_ERROR_PATTERNS`에 DB 에러 3개 추가 (DatabaseError, IntegrityError, ConnectionError)
- ImportError 세분화: `cannot import name` 패턴 별도 분리
- ModuleNotFoundError: "누락 .py 파일을 함께 생성하세요" 구체적 가이드

### 7. Phase 상태 관리 개선
- phase 실패 → [FAIL] 표시 (test_passed 확인)
- depends_on 스킵 전파 (스킵된 phase도 failed_ids에 추가)
- written_files reducer 추가 (재시도 시 파일 목록 누적)
- 파일 중복 카운팅 수정 (유니크 파일만 집계)

### 8. Procedural Memory 영속화
- `MemoryStore`에 JSONL 파일 기반 영속화 추가 (`{workspace}/.ai/memory/procedural.jsonl`)
- 테스트 실패→성공 패턴 자동 저장 (`_save_fix_pattern`)

### 9. 모델 업그레이드
- DashScope 기본 모델: Qwen 2.5 → **Qwen 3/3.5** 시리즈
  - REASONING: qwen3-max
  - STRONG: qwen3-coder-next (Agentic tool calling 최적화)
  - DEFAULT: qwen3.5-plus
  - FAST: qwen3.5-flash
- Claude/Anthropic 프로바이더 추가 (`langchain-anthropic` 설치)
- `create_chat_model()`에 anthropic 분기 추가

### 10. CLI 모델명 표시
- 노드 레이블에 실제 모델명 동적 표시: `✓ 코드 생성 (STRONG - qwen3-coder-next)`

## 현재 상태

### 벤치마크 결과 (칸반 보드 앱)
| 지표 | 5차 (Qwen 2.5, 초기) | 11차 (하네스 개선) |
|------|---------------------|-------------------|
| Phase 통과 | 3/4 | **4/4** |
| 소요시간 | 798.9s | **399.4s** |
| 재시도 횟수 | 10+ | **2** |
| 파일 정확성 | 34개 (중복) | **12개 (정확)** |
| 코드 품질 | 동작 불가 | **import 정합, D&D 구현** |

### 테스트: 354 passed (Docker 미실행 1개만 실패)

### 변경된 파일 (19개)
- `agents/coding_assistant/agent.py` — scaffold 경로, 에러 분류, 스마트 절단, 메모리 저장
- `agents/coding_assistant/prompts.py` — framework 필드 추가
- `agents/coding_assistant/schemas.py` — written_files reducer 추가
- `agents/orchestrator/agent.py` — skill_registry 전달, phase 상태, 파일 주입, dedup
- `agents/planner/prompts.py` — 테스트 가능 단위 분할 규칙
- `agents/planner/schemas.py` — MAX_FILES_PER_PHASE 8
- `cli/app.py` — 모델명 표시, 에러 라인 표시 개선
- `cli/session.py` — persist_dir 전달
- `core/memory/store.py` — JSONL 영속화
- `core/model_tiers.py` — Qwen 3 기본값, Anthropic 프로바이더
- `core/skills/registry.py` — framework_hint 필터링
- `data/skills/flask_vue.yaml` — v2.0 (cursorrules 병합)
- `data/skills/fastapi_react.yaml` — v2.0
- `data/skills/django_htmx.yaml` — v2.0
- `data/skills/react_express.yaml` — v2.0
- `pyproject.toml` — langchain-anthropic 추가
- `tests/test_cli.py` — 영속 메모리 테스트 수정

## 다음 단계 (제안)

1. **Qwen 3 실제 적용 확인**: 이번 커밋 후 재실행 시 Langfuse에서 qwen3-coder-next 등이 사용되는지 확인
2. **Orchestrator skill_registry 전달 재점검**: Langfuse에서 skill_context=[] 확인됨 — 수정이 반영 안 된 상태
3. **Claude 비교 테스트**: `LLM_PROVIDER=anthropic`으로 전환하여 동일 벤치마크 실행
4. **Procedural Memory 저장 확인**: `_save_fix_pattern`이 호출되는지 디버깅
5. **phase 실패 시 결과 표시 개선**: max iterations 도달해도 [OK]로 표시되는 문제는 수정 완료

## 중요 참고사항

- **Qwen 3 모델 미적용 상태**: 11차 실행은 코드 변경 전이라 여전히 qwen-plus(Qwen 2.5) 사용. 커밋 후 재실행 필요.
- **스킬 주입 미작동**: Orchestrator 경로에서 skill_context=[] 확인됨. 코드는 수정했으나 실행 시점에 미반영.
- **하네스 개선이 가장 큰 영향**: 모델/스킬 없이도 Phase 분할 + 파일 주입만으로 4/4 통과 달성.
- **Claude 전환 준비 완료**: `.env`에서 `LLM_PROVIDER=anthropic`만 변경하면 즉시 비교 가능.
- **MAX_FILES_PER_PHASE=8**: 프롬프트와 하드리밋 모두 업데이트됨.
