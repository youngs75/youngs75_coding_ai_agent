# 세션 인수인계: 2026-04-05 #0004

## 프로젝트 개요
- **프로젝트**: youngs75_coding_ai_agent (AI Assistant Coding Agent Harness)
- **목표**: Claude Code급 프로덕션 Coding AI Agent — Prime Jennie Self-evolving 코딩 하네스
- **저장소**: /home/youngs75/projects/youngs75_coding_ai_agent

## 이번 세션 작업 내역

### Claude Code "LLM 운영체제" 패턴 분석 및 다층 안전장치 도입

Claude Code 소스코드(`/home/youngs75/projects/claude-code-haha`)를 4개 관점에서 병렬 심층 분석:
1. 에이전트 루프 제어 (queryLoop, maxTurns, State 머신)
2. 토큰/컨텍스트 관리 (tokenBudget, autoCompact, diminishing returns)
3. 도구 시스템 (StreamingToolExecutor, 권한 계층, sub-agent spawning)
4. 가드레일 (Bash 보안 23개 체크포인트, System Reminder 주입, Stop Hooks)

분석 결과를 바탕으로 **3개 새 모듈** + **5개 기존 파일 수정** 구현:

#### 새 파일 3개
| 모듈 | 파일 | 역할 |
|------|------|------|
| **StallDetector** | `core/stall_detector.py` | 동일 도구+인자 반복 감지 → 2회 WARN / 3회 FORCE_EXIT |
| **TurnBudgetTracker** | `core/turn_budget.py` | LLM 호출 예산 (15회) + 감소수익 감지 (연속 3회 <500토큰 → STOP) |
| **AbortController** | `core/abort_controller.py` | asyncio.Event 기반 협력적 중단 (Ctrl+C → 우아한 종료) |

#### 수정 파일 5개
- `agents/coding_assistant/schemas.py` — `exit_reason: str` 필드 추가
- `agents/coding_assistant/config.py` — `stall_warn_threshold`, `stall_exit_threshold`, `max_llm_calls_per_turn` 설정 추가
- `agents/coding_assistant/agent.py` — 3개 모듈 통합:
  - `_parse_request()`: 턴 시작 시 stall_detector, turn_budget reset
  - `_execute_tools()`: 도구 호출 전 StallDetector 체크 → FORCE_EXIT 시 exit_reason 설정
  - `_execute_code()`: LLM 응답 후 TurnBudgetTracker 기록 → STOP 시 exit_reason 설정
  - `_should_use_tools()`: exit_reason 우선 체크 → GENERATE_FINAL로 즉시 라우팅
- `core/base_agent.py` — `abort_controller` 필드 추가 + `_wrap_node()` 중단 체크
- `cli/app.py` — AbortController 생성/주입, exit_reason 수집 및 한국어 메시지 렌더링

#### 테스트 3개 추가
- `tests/test_stall_detector.py` (11 tests)
- `tests/test_turn_budget.py` (9 tests)
- `tests/test_abort_controller.py` (9 tests)

## 현재 상태
- Phase 1~15 전체 구현 완료 + 다층 안전장치 도입
- 테스트: **767 passed** (기존 738 + 새 29), 1 failed (기존 flaky E2E), 43 skipped
- Ruff lint/format: All checks passed
- 그래프 토폴로지 변경 없음 (기존 노드/엣지 구조 유지)

## 다음 단계

### 우선 — CLI 스트리밍 렌더링 개선
현재 CLI는 에이전트 응답 전체를 반복 출력하는 것처럼 보임 (같은 내용이 여러 번 표시).

**조사 필요 사항:**
- `cli/app.py` `_run_agent_turn()`의 `astream_events` 처리 방식 분석
- `cli/renderer.py` CLIRenderer의 토큰 스트리밍 출력 방식 분석
- Claude Code 소스(`/home/youngs75/projects/claude-code-haha`)의 Ink 기반 TUI 렌더링 패턴 참고
- Codex 소스(`/home/youngs75/projects/codex`)의 TUI 렌더링 패턴 참고
- 특히 노드 전환 시 "이전 출력 다시 표시" 문제의 원인 파악
- Rich Live/Console 기반 incremental append 방식으로 전환 검토

### 기타
1. **`sudo rm -rf temp/`** 실행 (root 소유 파일 삭제)
2. **`.cli_history`를 `.gitignore`에 추가**
3. **Orchestrator 실사용 E2E 테스트** — 복합 작업 coordinate 검증
4. **MCP workspace 매핑 검증** — CLI 실행 위치와 Docker MCP 서버 workspace 정합성
5. **스킬 확장** — architecture_review, performance_review 등
6. **A2A 엔드포인트 활성화** — 실제 HTTP 위임 테스트

## 중요 참고사항
- **모델 (Qwen 시리즈)**: STRONG: qwen/qwen3-coder-plus (1M, 180s), DEFAULT: qwen/qwen3-coder-next (262K, 120s), FAST: qwen/qwen3.5-flash-02-23 (1M, 60s)
- **기본 에이전트**: orchestrator (모든 요청 자동 라우팅)
- **패키지 구조**: import는 `youngs75_a2a`. 패키지 변경 후 `uv pip install -e .` 필수
- **테스트 실행**: `source .venv/bin/activate && python -m pytest tests/ --ignore=tests/eval/ -v`
- **CLI 실행**: `youngs75-agent` (가상환경 활성화 필요) 또는 `uv run youngs75-agent`
- **초기 설정**: `make setup` (원커맨드)
- **다층 안전장치 설정**: `CodingConfig`에서 `stall_warn_threshold=2`, `stall_exit_threshold=3`, `max_llm_calls_per_turn=15` 조정 가능
